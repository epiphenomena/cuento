# DECISIONS.md — cuento

Why the design is the way it is. AGENTS.md defines *how* to work; PLAN.md defines *what* to build, in order; this file records *why*. When they conflict, this file wins for rationale.

Seeded at p00.1 from the settled-decisions table in PLAN.md. Later steps append one line per non-trivial choice (see AGENTS "Ambiguity"). Entries are append-only in spirit: supersede rather than silently rewrite.

## Settled decisions (D1–D26)

| ID | Decision | Rationale |
|----|----------|-----------|
| D1 | Amounts stored as `int64` in the currency's **minor units**; exponent per currency from `currencies` table. | Exact arithmetic; JPY/BHD-style exponents cost nothing to support. |
| D2 | **Net-debit** signed amounts (debits +, credits −). Every transaction sums to exactly 0 in its currency — and to 0 **within each fund** (D20). Signed vs DR/CR is display-only, per user. | Single column; mixed revenue/expense subtrees subtotal naturally. |
| D3 | Transactions are **single-currency**. Cross-currency flows = two transactions through a multicurrency **FX Clearing** account (equity-class). Its converted balance on reports is cumulative FX gain/loss. GnuCash-style per-split value/amount pair rejected for schema+UI complexity; revisit if clearing proves awkward. | Keeps the zero-sum invariant trivial and honest. |
| D4 | Audit = append-only `*_versions` tables: entity id + `change_id` + `valid_from` + `op(create/update/delete)` + full snapshot of business columns. Current tables are the denormalized latest state. State as of time T = row with max(`valid_from`) ≤ T per entity, excluded if `op='delete'`. Versions tables never see UPDATE/DELETE. | Fast current-state queries, mechanical point-in-time, pure append. |
| D5 | v1 audit surface: per-transaction history UI + per-entity as-of store queries + integrity check that current == latest version. Whole-report "books as edited at time T" is supported by the data but deferred as a feature. | Keeps the report toolkit single-plane for v1. |
| D6 | **sqlc** (queries) + **goose** (migrations, embedded). Forward-only migrations; runner backs up the db file before applying. No down migrations. | SQL stays visible/optimizable; backup beats theoretical rollbacks. |
| D7 | Driver: `modernc.org/sqlite` (pure Go). | CGO-free static binary, trivial cross-compile; perf ample at this scale. |
| D8 | Hosting: GCE **e2-micro always-free VM** + persistent disk + **Litestream → GCS** (free 5 GB). TLS via in-process autocert. Cloud Run rejected: ephemeral FS + scale-to-zero vs a single-writer SQLite file. | One binary, one VM, one systemd unit. |
| D9 | Auth primitives: scs sessions (SQLite store), argon2id, stdlib `http.CrossOriginProtection` (fallback nosurf), `html/template` escaping + strict CSP, `x/time/rate` on login. | Nothing hand-rolled; everything provable via route-registry tests. |
| D10 | Permissions: per-user `txn_perm ∈ {none,read,write}` + per-report-group read grants; `is_admin` implies everything. Report groups are declared in code and synced to the db at startup. Permissions are org-global in v1 (per-subsidiary grants → backlog, Q2). | Matches stated needs; no RBAC machinery. |
| D11 | Tree rules: A/L/E children must match parent type exactly; revenue and expense may interleave freely under R/E parents. Placeholders (accounts with children) hold no splits. | Balance sheet stays clean; mixed groupings stay possible — the chart holds *natural* categories, since programs are a dimension (D24). |
| D12 | FX conversion happens only in reports: balance-sheet figures at the closing (as-of) rate; P&L activity at each transaction-date rate; rate lookup = latest on-or-before date, direct pair then reciprocal. Rates stored as REAL; conversion rounds half-even at final aggregates. | Standard treatment; storage stays integer-exact. |
| D13 | Splits in a **finalized** reconciliation are locked (amount/account/txn/date/fund); editing requires an audited unreconcile first. Reconciliation is per `(account, currency)` and spans all funds — a bank statement covers one balance. | Statements stay provable; a key reason funds are a dimension, not currencies (D20). |
| D14 | **Bilingual UI (en, es) in v1**, extensible: `internal/i18n` embedded key→string catalogs + `t` template func; missing key falls back to en; a parity test enforces identical key sets across catalogs; adding a language = adding one catalog file. No CLDR or i18n dependency. Supersedes the earlier English-chrome-only decision. | The bookkeepers work in Spanish; catalogs are a page of code, not a framework. |
| D15 | Dependency allowlist: modernc.org/sqlite, pressly/goose/v3, sqlc (tool), alexedwards/scs/v2 (+sqlite3store), alexedwards/argon2id, golang.org/x/crypto (autocert), golang.org/x/time, google/go-cmp (tests). Additions need a DECISIONS entry *and* explicit human acknowledgment (AGENTS rule 1). | Lightweight by construction. |
| D16 | Date/number formats are small enumerations implemented and tested in `internal/money` — no CLDR dependency. Date entry uses text inputs (never `input[type=date]`). | Users pick from a handful of formats; browsers don't get a vote. |
| D17 | IDs are `INTEGER PRIMARY KEY AUTOINCREMENT` on financial tables (no rowid reuse after delete — audit-friendly). | Cheap referential permanence. |
| D18 | **Subsidiaries**: `subsidiaries` is a tree with exactly one root; each has a `base_currency`. Accounts map to **one or more** subsidiaries via `account_subsidiaries`, with the invariant **parent set ⊇ union of children's sets** (superset; assigning a sub auto-propagates to ancestors). Every transaction belongs to **exactly one** subsidiary and all its split accounts must include it. Report scope = a chosen subsidiary **consolidated with all descendants**; the root is full consolidation. Default report currency = the scoped subsidiary's base currency. | NetSuite-shaped without OneWorld machinery. |
| D19 | **Intercompany**: cross-subsidiary funding = paired transactions through due-to/due-from accounts flagged `intercompany`. Consolidated reports whose scope covers both sides collapse flagged accounts after asserting they net to zero per currency; a nonzero net renders as a warning row. Elimination journal entries are out of scope. | The flag makes consolidation honest and cheap. |
| D20 | **Restricted funds are a split dimension with fund-level conservation.** `funds` documents grants; a fund scopes to **one or more subsidiaries** via `fund_subsidiaries` (not inherited) and optionally to a **program subtree**. Every split carries `fund_id` (NULL = unrestricted); every transaction must sum to zero **within each fund**. Sub-currency modelling rejected: identical semantics but breaks single-statement reconciliation (D13), multiplies the currency/rate surface, and cross-currency conversion would mint pseudo-currencies. GAAP "released from restrictions" is **derived** in reporting, not journaled. | All the tracking power of the sub-currency idea, none of the collateral damage. |
| D21 | **Functional expenses (990 Part IX)** are a fixed enum `program | management | fundraising`. Expense accounts carry a **default** class; every expense split **requires** a class (prefilled, overridable); non-expense splits must be NULL. Trigger-enforced. Part IX is natural × functional — a matrix, so a dimension, not a duplicated hierarchy. | One report renders Part IX; the tree stays single and clean. |
| D22 | Import source is the **cleaned full-ledger CSV**. `cmd/ledgerimport` is driven by a reviewable mapping file: per source account → {type, parent, subsidiaries, default functional class}; per row → subsidiary and fund (defaults: root subsidiary, unrestricted) with optional source-column overrides. | The export is already a ledger; converting it beats re-keying history. |
| D23 | Negative restricted-fund balances (spending a grant past its receipts) are surfaced as **warnings** by `cuento check` and on fund pages — never blocked at write time. | Backdated entries make hard blocks hostile; visibility beats prevention. |
| D24 | **Programs are a dimension**: `programs` tree with a single seeded root ("General"); every **revenue and expense** split carries `program_id` (required; prefilled from the account default, else root); A/L/E splits carry none. Programs are org-global (Q5). The chart holds *natural* categories; mission structure lives in the program tree. Program and functional class are **orthogonal**. | 990 needs program-level revenue and expenses; duplicating programs in the tree was the same mistake as functional duplication. |
| D25 | **990 line mapping**: seeded `form990_lines` reference table (Parts VIII, IX, X); `accounts.form990_code` nullable with **effective code = own or nearest ancestor's** (inheritable). Not hard-required: 990 reports render an explicit **Unmapped** bucket instead of dropping rows; a warning check flags active R/E leaves with activity but no effective code. | One field, set ~10 times, and year-end becomes running four reports. |
| D26 | **Two-step go-live**: the production db is produced by `cmd/ledgerimport` from the cleaned CSVs + a reviewed mapping file, rehearsed during p09.4 (locally; mapping gitignored beside the source data). Deploy = (1) build db + `cuento check --strict`, (2) ship db + binary. The historical mapping assigns subsidiaries, funds, programs, functional classes, and 990 codes up front. | Import is the deployment path, not an afterthought. |

## Open questions (defaults in effect until answered)

- **Q5** — Programs scopeable per subsidiary vs org-global? Default: **global** (per-sub scoping → backlog). Affects p07, p08.
- **Q6** — Seed full Form 990 line set vs 990-EZ subset? Default: **full 990** (supersets EZ). Affects p05.1, p15.11.

Resolved (2026-07): Q1 (funds scope to subsidiaries, not inherited — D20), Q2 (permissions global — D10), Q3 (reports split with/without donor restrictions; per-grant = fund statement), Q4 (hosting, FX, "development" = fundraising column).

## D15 allowlist expansion — i18n + TOML (added 2026-07-12 at the human's request)

The human explicitly authorized adding a TOML parsing library and (conditioned on "lightweight, not taking over the project") an i18n library. Acknowledgment: 2026-07-12. Decision:
- Adopt **`github.com/nicksnyder/go-i18n/v2`** as the i18n engine, which brings **`github.com/BurntSushi/toml`** (the TOML grant) and **`golang.org/x/text`** (CLDR plural rules + language matching). go-i18n is library-shaped — it does not invert control; the boring-frontend/single-binary posture holds. `x/text` adds ~a few MB of CLDR tables to the CGO-free binary; accepted for correct en/es pluralization.
- This **supersedes D14's** "hand-rolled minimal parser, no CLDR/i18n dependency" clause and the p03.4 hand-rolled parser. The `internal/i18n` public facade (`T`, `Langs`, `{{t}}`, en-fallback, and the catalog-parity test) is **preserved**; catalogs move to the go-i18n TOML message format; pluralization is available where counts appear.
- These three modules are now on the D15 allowlist. No YAML lib is added (go-i18n's yaml unmarshaler is an unused transitive; catalogs are TOML).
- **Direct-require note (2026-07-12):** all three land as **direct** requires in `go.mod`, not the two-indirect shape the adoption task sketched. `go get ...go-i18n/v2/i18n` pulls only go-i18n + x/text (verified: it does **not** vendor BurntSushi/toml transitively). `internal/i18n` then imports `toml` directly (the prescribed `RegisterUnmarshalFunc("toml", toml.Unmarshal)`) and `x/text/language` directly (`NewBundle(language.English)` takes a `language.Tag`, no string form), so `go mod tidy` correctly marks all three direct. This is honest and reproducible (`make gen` stable); it deviates only from that task's descriptive "indirect" expectation, not from rule 1 — every module is D15-allowlisted.

## Functional testing (Playwright) — added 2026-07-11 at the human's request

The human explicitly asked for browser-based functional tests, **superseding AGENTS rule 12's original "no browser automation dependency" clause** (acknowledgment: 2026-07-11). Decisions:
- A **Playwright** end-to-end suite lives under `e2e/` (Node/npm, test-only). It is **not** a Go dependency (D15 untouched) and is **never** imported by the shipped binary or the frontend runtime — the boring-frontend rules (no framework/bundler/CDN, strict CSP) still govern the app itself.
- The suite drives the **real** `cuento serve -dev`: a fixture builds the binary, creates a temp SQLite db, seeds a known admin (via `cuento user add --admin`) plus any data a spec needs, launches serve on an ephemeral port, and tears it all down.
- Run via `make e2e` — **opt-in, separate from `make test`** (it needs a browser; `make test` stays hermetic per AGENTS). Chromium is provided by Playwright's own download.
- The suite **grows with the UI**: the harness (pE.1) is built now against the login flow (the only real page so far); every later UI phase (11–17) adds specs for its real user flows as part of that step's work. Functional coverage of a delivered UI flow is expected before its phase is considered done.

## Implementation notes (appended per step)

- **p00.2** — `make lint` runs golangci-lint's bundled gofumpt formatter *and* the standalone `gofumpt` binary; the two disagreed on import grouping (standalone splits the module-local `cuento/...` import into its own block, the bundled one merged it). Set `formatters.settings.gofumpt.module-path: cuento` in `.golangci.yml` so both produce the split form. Tooling reconciliation only; no design impact.
- **p00.3** — Pinned the Go toolchain to **1.25.12** (`go 1.25.12` in `go.mod`). govulncheck flagged 9 stdlib vulnerabilities (crypto/tls, crypto/x509, net, net/url, os, net/textproto) present in the locally-installed 1.25.7 and all fixed by ≤1.25.12; `GOTOOLCHAIN=auto` auto-downloads it. Keeps the vuln scan clean without touching any allowlisted dependency (D15). Bump the pin as later patches land.
- **p00.3 (DoD clarification, per human 2026-07-11)** — There is no hosted CI and the human manages pushes themselves. The definition of "green on the repo host" for **every** step in this project reduces to: `make lint test check` plus `go vet`, `golangci-lint`, `gofumpt -l`, `go test ./...`, `govulncheck ./...` all pass **locally**. The GitHub Actions workflow was **removed 2026-07-11 at the human's request** (they don't use GitHub CI); the local-green DoD stands unchanged — these are the checks to run locally before a step is "done".
- **p04.1** — Versioned tables seed with full audit consistency: a migration that seeds a versioned business row also inserts its `changes` row (actor = system user 1, fixed epoch timestamp `'1970-01-01T00:00:00Z'`, `kind='seed'`) and its `*_versions` `op='create'` row (`valid_from == changes.at`), so Z3/Z5 hold uniformly with no seed special-casing. The `subsidiaries_versions` snapshot column set (`parent_id, name, base_currency, active, sort_order`) is documented in the migration; p04.2's `InsertSubsidiaryVersion` query must write exactly those. Single-root is enforced by a **pair** of triggers (BEFORE INSERT + BEFORE UPDATE): on INSERT the AUTOINCREMENT `NEW.id` is unassigned, so the guard checks plain existence of another root (an `id <> NEW.id` clause would compare against NULL and never fire); on UPDATE it excludes the row with `id <> NEW.id`. The **program root (p07.1)** gets the same seed-with-version + single-root-trigger-pair treatment. **Deferred instance:** the system user (seeded in 00002 before `users_versions` existed) has NO version row yet — when **p06.1** creates `users_versions`, that migration MUST backfill a `op='create'` version row for user id 1 (excluding `password_hash`, rule 5) or Z3 will flag `users`.
- **p04.2** — Version-append convention established concretely (the pattern p05/p07/p08 copy): inside the funnel's `fn`, the entity op does its LIVE write FIRST, then appends the version row via a **snapshot-from-live** query (`InsertSubsidiaryVersion` = `INSERT ... SELECT` from the just-written live row), so the version row is byte-identical to the live row (Z3 cannot diverge) and `valid_from == changes.at` **by construction** — both structural, never asserted. **All validation lives inside `fn`** (cycle/root/active-children checks read the tx-bound `*sqlc.Queries`), so a rejected op returns its error out of `fn` and the funnel rolls the change row back — rejected ops leave **no audit trace** (asserted by the blocked-op no-trace tests). Deactivate is `op='update'` (the entity persists), never `'delete'`. Op mapping: create→`create`, rename/move/base-currency/deactivate→`update`. **Tooling quirk:** sqlc v1.31.1 **miscounts byte offsets** when a query `.sql` file contains multi-byte UTF-8 (e.g. em-dashes in comments), silently corrupting the generated SQL constants for the *whole* file (not just the offending query). Keep every `.sql` query file **pure ASCII**. Also: sqlc's sqlite parser rejects `sqlc.arg()`/`@name` and mangles numbered `?N` params inside an `INSERT ... SELECT ... WHERE` form, so `InsertSubsidiaryVersion` uses plain positional `?` (each param used once: op, change_id, entity_id) — the store wraps the resulting `Op/ID/ID_2` field names behind one `insertSubsidiaryVersion` helper.
- **p05.2** — Account ops copy p04.2's funnel/version discipline with three deliberate departures. (1) **Composite-key versions:** `account_names`/`account_subsidiaries` version rows key on `(account_id, lang)` / `(account_id, subsidiary_id)`; their `InsertAccountNameVersion`/`InsertAccountSubsidiaryVersion` copy `InsertSubsidiaryVersion`'s snapshot-from-live shape, changing only the `entity_id`/WHERE. `testutil.AssertVersionedName`/`AssertVersionedSub` filter on the composite key (the single-id `AssertVersioned` stays for `accounts`). (2) **Removal inverts live-write-first:** a membership removal is `op='delete'` whose snapshot is the *last-known* membership, so `removeSub` inserts the version row **before** deleting the live row (an add keeps the normal live-first order). (3) **Subsidiary-set asymmetry (D18):** `parent set ⊇ union of children's` is over the **account tree**, not the subsidiary tree — assigning subsidiary `A1` to an account does **not** add `A1`'s subsidiary-tree ancestors; report scoping consolidates descendants at read time (D18). **Create propagates, move rejects (resolved asymmetry):** `CreateAccount`/`SetAccountSubsidiaries` **auto-propagate** an assigned sub UP the account-ancestor chain (each ancestor missing it gains a membership + `op='create'` version; an ancestor already holding it is a no-op with no version row); `UpdateAccount` moves **reject** with `ErrSubMismatch` when the new parent's set does not cover the mover's. Removing a sub is blocked while a **direct child** still maps it (`ErrSubInUseByChild`); the split-usage half of that guard is **deferred to p08** (no `splits` table yet) as a tagged `// TODO(p08)` + a documented test note. **Functional-class-on-non-expense** is rejected cleanly with a typed `ErrFunctionalClassNotExpense` before the trigger fires (Build text said "validate cleanly" but named no error). **990 type check** matches the account's `type` against `form990_lines.account_types` by **CSV membership** (some lines allow >1 type), not equality (`Err990TypeMismatch`). **`Effective990Codes` resolves the nearest-ancestor code IN GO** from a flat `(id, parent_id, own_code)` fetch: sqlc v1.31.1's sqlite analyzer cannot resolve a *carried* recursive-CTE column in the output projection (it reports the carried column "does not exist"), so a value-carrying downward CTE is not expressible — the in-memory walk over the small chart is the store-wrapped alternative.
- **p06.1** — Credentials/perms/settings on `users` + the auth primitive. **The invariant (rule 5):** `users_versions` has **no `password_hash` column** and `InsertUserVersion` (snapshot-from-live) selects every users column **except** it, while `CreateUser`'s live `InsertUser` **does** write the hash — that asymmetry is the whole step, proven by `TestUsersVersionOmitsPasswordHash` (PRAGMA table_info has no such column; a `SELECT *` scan of the latest version row finds the secret in no column; the live row does hold it). **System-user version backfill (the p04.1-deferred instance):** 00006 inserts `changes` id **2** (the only prior change is id 1, 00004's root-subsidiary seed; 00003/00005 seed none) and a `users_versions` `op='create'` row for user id 1 via `INSERT...SELECT` from the just-ALTERed live row (so the snapshot equals the post-ALTER defaults by construction and cannot drift), keeping Z3/Z5 uniform — without any hash. `AssertVersioned` needed no change (single-id entity). **`report_groups`** is code-synced reference data (D10), **not** versioned (no twin) and **unseeded** here — the startup sync fills it in p06.3. **`user_report_grants`** + its composite version twin (`entity_id=user_id`, snapshot `group_name`, like `account_subsidiaries`) are schema-only this step: grant writers/version-append land in p13.2, so grant FKs are exercised by **direct SQL** tests, not a store method. **argon2id (`internal/auth`, D9/D15, the ONLY place it is called):** `Hash`/`Verify` wrap `argon2id.DefaultParams` (v1.0.0): Memory 65536 KiB (64 MiB), Iterations 1, **Parallelism = `runtime.NumCPU()`** (machine-dependent — encoded into each PHC string, so verification is self-describing; a fixed value for reproducible cross-host cost is a one-line future tuning), SaltLength 16, KeyLength 32. **`go get github.com/alexedwards/argon2id`** (allowlisted D15, no extra ack needed) is the only new **direct** dep; it transitively pulls `golang.org/x/crypto` (also D15) as **indirect**. That brings govulncheck finding **GO-2026-5932** (`golang.org/x/crypto/openpgp` unmaintained; Fixed-in: N/A) as an **uncalled/informational** finding — govulncheck exits 0, "code affected by 0 vulnerabilities" — because cuento never imports that subpackage; it does **not** block the DoD. **sqlc regen note:** after 00006 widens `users`, `GetUser`'s explicit-column projection makes sqlc emit a bespoke `GetUserRow` (was `sqlc.User`); the fields it scans are unchanged so the existing `sqlc/users_changes_test.go` still compiles. `InsertUserVersion` uses the `INSERT...SELECT...WHERE` positional-`?` form (generated `Op/ID/ID_2`), wrapped behind `insertUserVersion` like the subsidiary/account helpers; `users.sql` kept pure ASCII.
- **p05.3** — `Tree`'s name resolution became a **COALESCE fallback chain requested-lang → en → any** (D14), replacing the p05.2 `LEFT JOIN ... COALESCE(an.name,'')` in **both** `AccountTree` and `AccountTreeBySubsidiary` (updating only one is the obvious latent bug; the test exercises both the nil-filter and subFilter paths). The chain is three **correlated subqueries** on `account_names` (requested lang, then `'en'`, then `ORDER BY lang LIMIT 1` for the deterministic "any"), with a trailing `''` so an account with no names at all still resolves to a value. Two sqlc v1.31.1 sqlite-analyzer quirks shaped the final SQL: (1) an unaliased `account_names` inside a correlated subquery that also references the outer `tree.id` makes the analyzer flag `lang`/`name` as **"column reference ambiguous"** — each subquery gets its own table alias (`an1`/`an2`/`anx`) with every column qualified; (2) a bare `COALESCE(<subqueries>, '')` is typed by sqlc as `interface{}` (not `string`), which would break the `TreeRow.Name string` mapping, so the whole expression is wrapped in `CAST(... AS TEXT)` to pin the generated column to `string`. No store-Go change was needed (the mapping is unchanged); the `AccountTreeBySubsidiaryParams` fields (`Lang`, `SubsidiaryID`) kept their names despite the param **order** flipping (lang now precedes subsidiary in the SQL), and the store uses named-field structs so the reorder is invisible. **Test-data deviation:** the "any" case needs an account with *neither* the requested lang *nor* `en`, but `CreateAccount` mandates an `en` name and there is no remove-name API — so the test raw-`DELETE`s the seeded `en` row from `account_names` after creation (raw SQL in store tests is already in-convention, e.g. `nameAsOf`). The task's "build with CreateAccount/SetAccountName" cannot otherwise reach the any-branch.
- **p06.2** — Web sessions + login + the security middleware spine every later web step inherits (D9, rule 13). **`Handler` signature changed** to `Handler(cfg web.Config, db *sql.DB, st *store.Store)` (Config carries `Version` + `Dev`); the p00.2 `Handler(version)` callers/tests were updated. `serve` now `db.Open`s after migrating (it needs the `*sql.DB` for both the store and the scs store) and gained a **`-dev`** flag (the Makefile `run` target already passed it). **Middleware chain (outer→inner, so headers wrap even deep errors and cross-origin rejects before any work):** `secureHeaders → crossOrigin (stdlib http.CrossOriginProtection) → scs LoadAndSave → auth (resolve current user from session id, re-read each request so perm/locale changes need no re-login; missing/disabled ⇒ anonymous, not error) → lang (user locale → ?lang query → lang cookie → en)`. **scs session table:** created by goose migration **00007** (`sessions(token TEXT PK, data BLOB NOT NULL, expiry REAL NOT NULL)` + `sessions_expiry_idx`) so goose stays canonical (rule 4) — NOT scs auto-create. Schema **verified against the installed library source** `github.com/alexedwards/scs/sqlite3store@v0.0.0-20251002162104-209de6e426de` (its `Find`/`Commit`/`Delete` raw SQL uses `token`/`data`/`expiry` with `expiry` a `julianday()` REAL; default table name `sessions`); matches the library README DDL exactly. `sessions` is scs-owned infra, **not** a versioned business table (no `_versions` twin, no `changes` wiring, no seed). **Cookie:** name `cuento_session`, `HttpOnly`, `SameSite=Lax`, `Path="/"`, `Secure = !Dev`, 12h Lifetime / 2h IdleTimeout. **Uniform auth error / no enumeration (rule 13):** unknown user, wrong password, AND disabled account all return the **same** 401 + identical body (`auth.invalid`). Timing is equalized too, not just the body: a fixed **decoy argon2id hash** (built once at startup via `auth.Hash`) is verified-and-discarded on the unknown-user / passwordless / DB-error paths so every attempt spends one argon2 verify regardless of whether the username exists. On success: `RenewToken` (session-fixation defense) then `Put(user_id)`. **CSP** (strict, no inline script, rule 12): `default-src 'self'; script-src 'self'; style-src 'self'; img-src 'self'; connect-src 'self'; font-src 'self'; object-src 'none'; base-uri 'none'; frame-ancestors 'none'; form-action 'self'` — plus `X-Content-Type-Options: nosniff`, `Referrer-Policy: same-origin`, `Cross-Origin-Opener-Policy: same-origin`, `X-Frame-Options: DENY`. HTML `no-store` is set by the HTML render path (`render`), NOT the header middleware, so `/static` + `/healthz` keep their own caching (p10 owns asset headers). **Rate limit** (`x/time/rate`): keyed by **IP+username**, burst 5, refill 1 token/30s, checked **before** any argon2 work (so throttling actually saves the hashing cost); over-limit ⇒ **429** with the login page + `auth.rate_limited`. Limiter map is bounded (cleared wholesale at 4096 keys — the simplest bound that only ever loosens, never locks out). Client IP = direct `RemoteAddr` only (never a spoofable forwarded header). **`{{t}}` per-request localization:** templates parse once with a stub `t`; `render` clones the set and rebinds `t` to a closure over the resolved lang before Execute (html/template's func map is what carries per-request state), so `?lang=es` renders the Spanish catalog. **New login keys** (`auth.login_title/username/password/sign_in/invalid/rate_limited`) added to BOTH catalogs (parity test stays green). **"Subsequent request authenticated" tested without p06.3 routes:** `GET /login` redirects (303) when identity resolves, so the success test asserts the cookie authenticates without mounting a throwaway route that p06.3's registry-completeness test would later reject. **Store reads added** (rule 2 permits reads via sqlc): `CredentialsByUsername` (`UserByUsername` query) and `UserByID` — both NEW queries; `GetUser` untouched (its projection is pinned by `sqlc/users_changes_test.go`). **Deps:** `scs/v2`, `scs/sqlite3store`, `x/time` added (all D15-allowlisted, no extra ack); `go mod tidy` flipped them direct. No non-allowlisted direct dep (sqlite3store's own test-only `mattn/go-sqlite3` is a go.sum build-list entry, not in our require graph). govulncheck exit 0 (the single uncalled `x/crypto/openpgp` GO-2026-5932 finding from p06.1 persists, informational). All `.sql`/migration files kept pure ASCII (p04.2 quirk).
- **p06.3** — The route registry + provable enforcement every later web step plugs into (rule 8, D10). **`Perm`** is a small value type (`{kind, group}`), not an interface, so `routes()` is a plain declarative table and the matrix switches on it exhaustively: markers `Public/AnyUser/TxnRead/TxnWrite/Admin` (package vars) + the `ReportGroup(name)` constructor (the only variant carrying data — a report route's Perm is its group, Appendix B). **`Route{Method, Pattern string; Perm; Handler http.Handler}`** — `http.Handler` not `http.HandlerFunc` so the static `FileServer` mounts without wrapping. **Single registry / single mount:** `(s *server).routes()` declares EVERY route (healthz/static/login GET+POST/logout migrated in from p06.2's manual `mux.HandleFunc` block, which is deleted) plus a minimal authenticated landing; `(s *server).Mount()` is the ONLY site a route attaches to a mux — it iterates `routes()`, wraps each handler in `enforce(perm, h)`, and wraps the whole mux in the p06.2 security `chain()` (so `auth` populates the current user before `enforce` reads it). `NewApp` now just calls `Mount()`. No route exists outside `Mount` — the "mux corresponds exactly to the registry" guarantee is **structural** (Go's `http.ServeMux` has no enumeration API), backed by the reachability + catch-all-404 checks. **Landing = `GET /{$}`** (exact-match anchor), deliberately NOT `GET /`: a bare `/` is a catch-all that swallows every unmatched GET and defeats `TestRouteRegistryComplete`'s unregistered-path-404 assertion. It renders `home.tmpl` (new i18n keys `home.title/home.welcome`, `auth.sign_out`, in BOTH catalogs — parity green). **Enforcement policy, encoded ONCE in the pure `decide(perm, user, hasGrant) outcome`** (D10, `is_admin` short-circuits everything before any perm/grant check): Public→everyone; else anon→`RedirectLogin`; then AnyUser→allow, TxnRead→`{read,write}`, TxnWrite→`write`, ReportGroup→`hasGrant(name)`, Admin→(non-admin already excluded); else `Forbid`. **The 302/303 split is load-bearing:** enforcement's anon→/login bounce uses **302** while every handler redirect (logout, already-authed `/login`) uses **303**, so the matrix's `Allow` check (`!=403 && !(302→/login)`) catches "authorized user wrongly bounced to login" yet lets logout's legit 303→/login pass — without the split, `Allow` could only be `!=403` and would miss that bug. **Grant resolution is lazy:** `grantChecker` queries `store.ReportGrants` ONLY for a `ReportGroup` route with a concrete user (anon/admin already decided; non-report perms never call it), so `authMiddleware`'s hot path (every request incl. /healthz, /static) never loads grants; a grant-read error **fails closed** (denies). Grants live on neither `CurrentUser` nor the session — resolved per-request. **Matrix auto-extends from the registry:** `TestPermissionMatrix` iterates `srv.routes()` (never a hardcoded list) × 6 personas and asserts the real handler's response `outcomeMatches` what `decide` computes from the route's Perm — so a route APPENDED to `routes.go` in p11+ is enforced-and-checked with zero test edits. `decide` is the single source of truth (no re-expressed expectations); `TestDecidePolicy` proves `decide` itself across all 6 Perms × 6 personas **including `ReportGroup`**, whose HTTP coverage waits for p15's report routes (the registry has none yet — "do not invent feature routes"). **Personas driven by SESSION INJECTION, not login** (task sanctions "inject the session/user"): `mintCookie` writes a fresh scs session row per request (`Put(user_id)` under the same key `authMiddleware` reads, `Commit`), so the matrix pays no argon2/rate-limit cost AND logout's `Destroy` can't contaminate later requests in the sweep (each carries its own token) — the route-outer/persona-inner loop would otherwise have logout blank every persona before `/{$}` is reached. **Report-group startup sync (D10):** `web.SyncReportGroups(ctx, st)` (the web package owns the code-declared set because route Perms reference the names) → `store.SyncReportGroups` does an **idempotent `UpsertReportGroup`** per name — OUTSIDE the write funnel, like currencies (`report_groups` is reference data, NOT versioned: no `changes`/`_versions` wiring, rule 2 permits reference upserts via sqlc). Wired into `serve` after `db.Open`, before listening; safe every boot; nothing is deleted (a vanished group keeps its FK). **Reversible choice recorded:** the code-declared set is a single placeholder `_placeholder` (`codeReportGroups()`) until p15 declares the real report groups — the mechanism is real, the content is a stub; `TestPermissionMatrix`'s ReportsOnly persona is granted it via **direct SQL** (grant writers land in p13.2; raw SQL in tests is in-convention, p05.3). **Store additions (reads/reference-upsert only, no funnel):** `ReportGrants(userID)` (new `ReportGrantsForUser` query) + `SyncReportGroups`/`UpsertReportGroup`; `report_groups.sql` kept pure ASCII. No new dependency (scs already direct from p06.2); `go.mod`/`go.sum` unchanged; govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p06.4** — The `cuento user add|passwd|disable` CLI + the serve bootstrap hint. **Password input = one line from STDIN** (a prompt to stderr only when stdin is a char device; a piped line otherwise), NOT a `-password` flag: a flag leaks the secret into shell history/process args, while a stdin line works interactive AND scripted/tested. The factored funcs (`userAdd(ctx, st, username, display, admin, password)`, `userPasswd(ctx, st, username, password)`, `userDisable(ctx, st, username)`) take the password as a plain string so tests call them directly with a real migrated temp db, bypassing I/O; empty password rejected. **System actor bound INSIDE the factored funcs** (`store.WithActor(ctx, store.Actor{ID:1})`), not just in dispatch, so the guarantee holds for any caller and tests pass a plain `context.Background()`. **Two new versioned store methods**, both op='update', both reusing the EXISTING `insertUserVersion(..., "update", id)` snapshot-from-live helper so rule-5 hash exclusion is automatic (no new version query): `SetUserPassword` (live `UPDATE users SET password_hash` — the new secret enters only the live table; `TestSetUserPasswordVersionOmitsHash` proves the snapshot omits it) and `DisableUser` (live `UPDATE users SET disabled_at = now`; disabled_at IS in the snapshot so the audit trail records the disabling). New sqlc queries are plain `UPDATE`/`SELECT COUNT`/`SELECT id` (the p04.2 `INSERT...SELECT` numbered-param quirk does not apply); `users.sql` kept pure ASCII. Also added reads `UserIDByUsername` (passwd/disable take a username; a miss → typed `ErrUserNotFound`, a clean CLI message not a panic) and `CountHumanUsers` (`WHERE id <> 1`). **`user add` defaults:** `txn_perm='none'` (admins imply all, D10; per-perm management is the admin UI p13.2), `--display` falls back to the username. **Flag handling — interspersed parse (`parseInterspersed`):** stdlib `flag` stops at the first positional, so `user add carol --admin` silently dropped `--admin` and `user passwd frank -db /p` silently used the DEFAULT db (a silent wrong-target write); the loop re-parses around each positional so flags work in any position and a value-taking flag (`--display "Name"`) is never mistaken for the username. `parseUserAdd` is factored pure + unit-tested (`TestParseUserAddInterspersed`) to lock the regression — the end-to-end tests bypass the parser, so the `--admin` bug only bites at the flag layer. **Bootstrap hint:** `serve` calls `st.CountHumanUsers`; zero ⇒ `log.Printf` a pointer to `cuento user add <username> --admin`. It is operator-facing console output (same class as the existing serve log lines), NOT a UI string — no i18n catalog entry; a lookup error is non-fatal (never block startup on a hint). **Known pre-existing quirk (NOT p06.4, out of scope):** `db.Open` (p01.1) `url.PathEscape`s the path into the `file:` DSN, so an ABSOLUTE `-db /path` escapes `/`→`%2F` and modernc resolves it relative to cwd — harmless within one process (tests use absolute temp paths and pass because migrate+open share the cwd) but means cross-invocation `-db` with an absolute path is cwd-sensitive; the default db path works end to end. Not touched here (would mask a real p01.1 bug). No new dependency (bufio/flag/io are stdlib); `go.mod`/`go.sum` unchanged; `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p07.1** — Programs (D24), the second single-root dimension, are structurally **subsidiaries MINUS base_currency**: `00008_programs.sql` copies 00004's shape — the live table, the `programs_versions` twin (snapshot cols `parent_id, name, active, sort_order`), the single-root **trigger pair** (BEFORE INSERT checks plain existence since AUTOINCREMENT `NEW.id` is NULL; BEFORE UPDATE excludes `id <> NEW.id`), and the **audit-consistent seed** of root "General" (id 1). The seed's `changes` row is **id 3** (id 1 = 00004 root-subsidiary seed, id 2 = 00006 system-user backfill; 00003/00005/00007 seed no change). **Distinct names throughout (package `store` and the sqlc query package are shared namespaces):** sqlc queries `InsertProgram/GetProgram/UpdateProgram/InsertProgramVersion/ProgramTree/ProgramDescendants/CountActiveProgramChildren` (NOT the subsidiary `SubTree/Descendants/CountActiveChildren`); `*Store` methods `ProgramTree()`/`ProgramDescendants(id)` (a `Descendants` method already exists); errors `ErrProgramSecondRoot/ErrProgramParentMissing/ErrProgramRootImmovable/ErrProgramHasActiveChildren/ErrProgramNotFound`. **`ErrCycle` is REUSED** (same meaning; accounts.go already does). Deactivate mirrors all four subsidiary reject paths incl. the active-children block — "blocks new use only" is the op='update'-not-'delete' *semantic* (history intact; the split-based blocks-new-use assertion is tagged for p08), not the absence of a children guard. **`default_program_id` accounts ripple (the correctness core):** 00008 `ALTER accounts ADD COLUMN default_program_id INTEGER REFERENCES programs(id)` (the column deferred from 00005) AND `ALTER accounts_versions ADD COLUMN default_program_id INTEGER` — the version snapshot must capture it or **Z3 (current == latest snapshot) diverges** for any account touched after p07.1. Four `accounts.sql` queries updated: `InsertAccount` (add param), `UpdateAccount` (add to SET), `InsertAccountVersion` (add to the snapshot SELECT), and `GetAccount` — the last projects `default_program_id` **LAST** to match the table's PHYSICAL column order (ADD COLUMN appends), so sqlc keeps mapping the result to the `sqlc.Account` model rather than emitting a bespoke `GetAccountRow` that `accounts.go`'s `GetAccount() (sqlc.Account, ...)` and `validateMove` depend on. **Silent-NULL regression guarded:** `UpdateAccount`/`DeactivateAccount` do read-modify-write; `next := cur` carries `cur.DefaultProgramID`, and every `UpdateAccountParams` literal (3 call sites) now maps `DefaultProgramID`, so an unrelated update never NULLs it — proven by `TestAccountDefaultProgramREOnly` (set on a revenue acct, update an unrelated field, assert both live + latest-snapshot still hold it). **Default-program R/E-only enforcement (D24):** `CreateAccount`/`UpdateAccount` take an optional `DefaultProgramID`; non-R/E → `ErrDefaultProgramNotRE` (checked early in Create before the tx); inside `fn`, `checkDefaultProgram` validates existence (`ErrDefaultProgramMissing`) and active (`ErrDefaultProgramInactive`) — **two error names beyond the task's `ErrDefaultProgramNotRE`**, the minimal split for "missing" vs "inactive". `UpdateAccount` treats a non-nil **0** as CLEAR (NULL), a positive value as set. All existing account/subsidiary tests still pass (the ripple is column-append-only; no test asserted a fixed accounts column count). Migration + `programs.sql` + `accounts.sql` kept pure ASCII (p04.2 byte-offset quirk applies to migrations too since sqlc reads them as schema). No new dependency; `go.mod`/`go.sum` unchanged; `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p07.2** — Funds (D20), the restricted-fund split dimension, are schema-only this step (`00009_funds.sql`; store ops are p07.3). `funds` has the `restriction IN ('purpose','time','perpetual')` CHECK, `program_id -> programs(id)` FK (optional program-subtree scope), and **date GLOB CHECKs** on `start_date`/`end_date` (`IS NULL OR ... GLOB '[0-9][0-9][0-9][0-9]-[0-9][0-9]-[0-9][0-9]'`). `fund_subsidiaries(fund_id -> funds, subsidiary_id -> subsidiaries, PK (fund_id, subsidiary_id))` carries the scope; the **>=1-subsidiary minimum is store-enforced (p07.3), not a schema constraint**. **No seed row** (unrestricted = absent `fund_id`, D20; no "general fund"): so **no `changes` row and no seed version rows** this migration -- the last change id stays 3 (00008's program seed), and there is no seed-consistency test. **Two version twins:** `funds_versions` is the standard single-id shape (entity_id = fund id; snapshot cols `name, funder, purpose, restriction, program_id, start_date, end_date, notes, active`); `fund_subsidiaries_versions` is the **composite shape like `account_subsidiaries_versions`** (entity_id = fund_id, snapshot `subsidiary_id`, op create/delete for membership -- no 'update'; index `(entity_id, subsidiary_id, valid_from)`) -- documented in the migration for p07.3's `InsertFundVersion`/`InsertFundSubsidiaryVersion`. Both twins carry **no CHECK/FK on snapshot columns** (only `change_id -> changes`), matching `accounts_versions`: a snapshot records history, it does not re-validate it. **Deviation from the task's listed date test (AGENTS method-1):** the task listed `start_date='2025-13-99'` as a rejection sample, but the authoritative `transactions.date` GLOB (Appendix A) is **shape-only** (digit count + dashes), which the task also specified verbatim -- and `'2025-13-99'` matches that shape (month/day are still digits), so it is *accepted*. Rather than diverge from `transactions.date` (rule 4: migrations are truth; schema consistency), the loose pattern was kept and the rejection sample swapped for a shape-invalid string (`'2025-1-1'`, wrong digit count); `'not-a-date'` rejects, `'2025-01-01'`/NULL accepted. `make gen` regenerated only the four expected `sqlc` models (`Fund`, `FundSubsidiary`, `FundsVersion`, `FundSubsidiariesVersion`) -- sqlc parsed all CHECKs/GLOBs without error, no query files needed (direct-SQL tests). Migration + test kept pure ASCII (00008 convention). No new dependency; `go.mod`/`go.sum` unchanged; `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **pE.1** — The Playwright functional-test harness (`e2e/`, test-only; the "Functional testing" section above is the settled decision, this is the build note). **Pinned `@playwright/test@1.53.2`** — chosen *empirically*, not from memory: the pre-installed browser is `~/.cache/ms-playwright/chromium-1179`, and each candidate version's `node_modules/playwright-core/browsers.json` was read for its bundled `chromium.revision` until it matched **1179** (1.49.1→1148, 1.52.0→1169, 1.53.0→1178, **1.53.1/1.53.2→1179**, 1.54.0→1181). `npx playwright install --dry-run chromium` confirms 1.53.2 resolves to the cached `chromium-1179` (chromium 138.0.7204.23), so `npm ci` downloads **no** browser. Bump the pin only to another version whose bundled revision still matches a cached browser, else a browser download appears in `make e2e`. **Fixture design (`e2e/fixtures.js`, worker-scoped `server` fixture):** free port allocated in **Node** (listen on `127.0.0.1:0`, read the assigned port, close — NO Go change to resolve the port, per the task's preferred approach); a temp dir + db under `os.tmpdir()`; then `cuento migrate -db <db>`, `cuento user add e2eadmin --admin -db <db>` with the password piped on **stdin** (newline-terminated; the CLI reads one line), and `cuento serve -dev -db <db> -addr 127.0.0.1:<port>`; readiness by polling `GET /healthz` (Public, no auth) for 200; teardown SIGTERMs serve (SIGKILL after 5s) and removes the temp dir. **The p06.4 `db.Open` cwd-sensitivity quirk is handled structurally:** all three child processes (migrate / user add / serve) are spawned with the **same cwd (repo root) and the same `-db` value**, so `url.PathEscape` resolves them to one physical file — otherwise `user add` seeds a db that `serve` never opens and a correct password returns `auth.invalid`. `bin/cuento` is built **once** via `globalSetup` (`make build`, idempotent). **`login.spec.js` drives the REAL page** (`templates/login.tmpl`): selectors `#username` / `#password` / the submit `<button>`; the uniform error is `p[role="alert"]` (rendered `{{t .Error}}`, key `auth.invalid`). Three cases: wrong password → alert visible + still on `/login`; unknown user → same uniform error (no enumeration); valid `e2eadmin` creds → 303 to `/` (authenticated `home.tmpl` landing, no `#password` present). **Opt-in via `make e2e`** (builds the binary, `cd e2e && npm ci && npx playwright test`) — deliberately **OUT of `make test`** (which stays hermetic: no browser, no network per AGENTS). `e2e/node_modules`, `test-results`, `playwright-report`, caches gitignored (`e2e/.gitignore`); `package.json` + `package-lock.json` + config + specs + fixture committed. **No Go code touched, no Go dependency added** (Playwright is Node-only, D15 untouched); `go.mod`/`go.sum` unchanged; `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists). `npm audit` reports advisories in Playwright's own dependency tree — test-only, never shipped or imported by the binary/frontend (rule 12), so they do not bear on the Go build.
- **p07.3** — Fund store ops (`internal/store/funds.go`, D20). Copies the account composite-membership discipline (p05.2) but is **deliberately simpler**: `fund_subsidiaries` is a **FLAT set** (>=1, store-enforced by `ErrFundNoSubsidiary`) with **NO ancestor propagation and NO superset invariant** — there is no `addSubWithPropagation` analogue; `addFundSub` inserts a membership directly then versions it op='create' (live-write-first), and `removeFundSub` versions op='delete' **BEFORE** the live DELETE (the snapshot-from-live removal ordering, mirroring accounts). Both the fund row and every membership are written under **ONE** `write()` change (`TestCreateFundVersioned` asserts change count +1). **`UpdateFundInput.Subsidiaries` set-semantics (the choice):** nil = leave the set unchanged; a non-nil slice = the full DESIRED set, diffed against the current set (adds op='create', removes op='delete'), and an empty/deduped-empty desired set is rejected with `ErrFundNoSubsidiary` (>=1 must remain) — chosen over an add/remove-delta shape because desired-set matches the accounts `SetAccountSubsidiaries` idiom and makes "still >=1 after the change" a single check. **Program scope** reuses the accounts `*int64` convention (nil unchanged / non-nil 0 clears / positive sets) and is validated **existence-only** via the existing `GetProgram` (task: "must exist if set" — no active check, which would be untested gold-plating); a missing program is `ErrFundProgramMissing`. **p08-deferred split guards (NOT silently skipped):** `UpdateFund` carries a `// TODO(p08)` before each membership removal (block removing subsidiary S while splits reference the fund in S — splits table absent until p08.1); `TestNarrowSubsBlockedBySplits` is `t.Skip`-tagged with the matching TODO, and `TestCloseFundBlocksNewUse` is tagged (the post-time block is p08) but asserts the close is versioned + active=0 now. **`CloseFund`/`ReopenFund`** are op='update' (never 'delete', rule 14) sharing a `setFundActive` body. **`ActiveFunds(subsidiaryID)`** JOINs funds+fund_subsidiaries filtered by `active=1 AND subsidiary_id=?`, `ORDER BY funds.id` (deterministic option source, D20/Q1). **Deviation from the listed test set (AGENTS method-1):** the task/PLAN listed `TestCreateRequiresAtLeastOneSub`, but p05.2 (accounts) already declared that exact name in the shared `store` package — renamed to `TestCreateFundRequiresAtLeastOneSub` (a compile-forced disambiguation; the DoD requires distinct names). `testutil.AssertVersionedFundSub` added (mirrors `AssertVersionedSub`, filters `entity_id=fund_id AND subsidiary_id`). `funds.sql` pure ASCII; generated version params wrapped behind `insertFundVersion` (`Op/ID/ID_2`) and the composite membership helpers (`Op/ID/FundID/SubsidiaryID`). No new dependency; `go.mod`/`go.sum` unchanged; `make gen` reproducible, `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p08.2** — Transaction post/update/delete (`internal/store/transactions.go`, D2/D18/D20/D21/D24) — the CORE financial logic. **Validate-on-tx-bound-q (the TOCTOU close):** all validation runs inside the funnel's `fn` on the tx-bound `*sqlc.Queries` the funnel passes, NOT on `s.q` before `write()`. `validateAndResolve` loads currency/subsidiary/accounts/funds/programs on the SAME tx that inserts, so an account deactivated or a fund closed between validate and insert cannot slip through. **Shared validate+resolve for Post AND Update:** one function returns `[]resolvedSplit` (program/functional class DEFAULTED before insert so the triggers never fire on the happy path); Update diffs RESOLVED values (not raw input) against the live split — an omitted-but-already-defaulted program/class counts as *unchanged* and gets no version row (the subtle `TestUpdateDiffsSplits` trap: a naive raw-input or re-version-everything diff passes balance tests but fails the per-split version-row COUNT assertion). **Two-tier zero-sum + error order:** overall sum==0 is checked FIRST (`ErrUnbalanced`), THEN per-fund-group sum==0 (`ErrFundUnbalanced`, NULL fund_id = the single "unrestricted" group, handled explicitly in a Go map keyed by 0). Per-fund-zero implies overall-zero, so overall must trip first or the two errors are indistinguishable; the two imbalance fixtures reach distinct errors. **Program-vs-function defaulting asymmetry (D24 vs D21):** program ALWAYS resolves for an R/E split (input -> account `default_program_id` -> **root program looked up by `parent_id IS NULL`**, never hardcoded id 1) then must be active (`ErrInactiveProgram`); A/L/E splits must carry no program (`ErrProgramOnBalanceSheet`). Functional class CAN fail: an expense split with no class and a NULL account default raises `ErrExpenseNeedsFunction` (p05.1's trigger bans class on non-expense but does NOT require it on expense, so an expense account can have a NULL default — this typed error, not the trigger, is the real content of `TestPostExpenseRequiresFunction`); a non-expense split with a class -> `ErrNonExpenseFunction`. Fund-program subtree scope (`ErrFundProgramScope`) applies to **R/E splits only** (the A/L/E cash side tagged the fund carries no program and is fine); the subtree membership is resolved in Go from `ProgramSubtreeIDs` (sqlc's sqlite analyzer rejects a recursive-CTE alias in an outer EXISTS/WHERE — same limitation as `Effective990Codes`). **Update-diff rule:** replace-set by split id — present&changed -> live update + `splits_versions` op='update'; present&identical -> NO version row; existing-but-absent -> live DELETE + op='delete' (captured BEFORE the delete, snapshot-from-live removal order); no id -> insert + op='create'. The txn header ALWAYS gets an op='update' version on an edit (the change row anchors the edit; keeps the header as-of a clean LIMIT-1 lookup) — a consciously-chosen consistency point. **Soft-delete (rule 14):** `DeleteTransaction` sets `deleted=1` + ONE `transactions_versions` op='delete'; splits are left untouched (no split delete-versions) — the as-of query excludes the txn by its own delete row. **As-of reconstruction (D4/D5):** `TransactionAsOf(id, at)` reads the header via `TransactionVersionAsOf` (latest `valid_from<=at`, absent/excluded if op='delete') and the split SET via `SplitVersionsAsOf` (all split versions for the txn with `valid_from<=at`, ordered `entity_id, valid_from DESC, id DESC`), then takes the FIRST row per `entity_id` in Go (dropping op='delete') — resolved in Go, like `Effective990Codes`, so every SQL param is single-use (avoiding the p04.2 numbered-param quirk on reusing `at`). Tiebreak matches `AssertVersioned`. `TestTransactionAsOf` uses `WithClock` (post@T0, edit1@T1, edit2@T2) and asserts the reconstructed split SET membership AND each split's values at the middle state. **Date validation:** the schema GLOB is shape-only, so the store parses the date with `time.Parse("2006-01-02")` and round-trips it (rejects `2025-13-99` etc. with `ErrBadDate`) before insert. **Completed p08 guards (grep `TODO(p08)` in `internal/` now CLEAN):** `SetAccountSubsidiaries` (accounts.go) blocks removing sub S from account A while `SplitUsesAccountInSubsidiary` finds a split on A in a non-deleted txn of S (`ErrSubInUseByChild` extended); `UpdateFund` (funds.go) blocks removing S from fund F while `SplitUsesFundInSubsidiary` finds a split tagged F in a non-deleted txn of S (new `ErrFundSubInUseBySplit`). `TestNarrowSubsBlockedBySplits` un-skipped; the split half of `TestRemoveSubBlockedByChildOrSplits` un-skipped (via `testRemoveSubBlockedBySplit`). **Concurrency finding: PASSES AS-IS, no `BEGIN IMMEDIATE` needed.** Verified: `db.Open` sets `MaxOpenConns(4)` (genuine overlap possible, not a trivially-serial pool of 1) + WAL + `busy_timeout` 5000ms; the funnel writes the `changes` row FIRST with no in-tx read before it (store.go), so concurrent posters serialize on the first write via WAL's single-writer + busy_timeout rather than deadlocking read-then-write. `TestConcurrentPostsSerialize` (8 goroutines, channel barrier) is green under `-race -count=5` with distinct change ids and no "database is locked". **Minimal payee support:** `CreatePayee(name)` (versioned op='create'); payee_id optional on the txn (autocomplete is p12.3). **Currency-active** has a named requirement but no listed test — smallest choice `ErrInactiveCurrency`, no bespoke test added. `transactions.sql` pure ASCII; version-append params wrapped behind `insertTransactionVersion`/`insertSplitVersion`/`InsertPayeeVersion` (`Op/ID/ID_2`). No new dependency; `go.mod`/`go.sum` unchanged; `make gen` reproducible, `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p08.3** — The integrity suite (`internal/ledger`, `cuento check`), AGENTS rule 7's "verified by cuento check" half. **Structure:** `Check(ctx, db) ([]Violation, error)` runs a reviewed registry `[]check{Rule, Severity, SQL}` (in `checks.go`) of **const SQL strings** — the ONE sanctioned exception to sqlc-only (rule 6), static and reviewed here as a set. Every check SQL returns exactly ONE text column (a detail naming offending ids) and one row per violation; no rows == passed. `Violation{Rule, Severity(Error|Warning), Detail}`; `HasErrors` drives the exit code. **Rules implemented now:** Z1 (txn sums to 0), Z2 (splits on leaves), Z3 (current == latest version snapshot, ALL versioned tables incl. composite `account_names`/`account_subsidiaries`/`fund_subsidiaries` and a dangling-membership branch), Z4 (`pragma_foreign_key_check()` table-valued fn — the modernc driver supports it), Z5 (every `*_versions.change_id` -> real change, all 12 twins incl. `user_report_grants_versions`), Z6 (orphan splits), Z7 (account tree acyclic, bounded upward walk), Z10 (per-fund-group zero-sum, NULL=unrestricted via `COALESCE(fund_id,-1)`), Z11 (split account mapped to txn sub), Z12 (parent sub-set superset of children), Z13 (fund scoped to txn sub), Z14 (functional_class iff expense), Z15 (program iff R/E + inside fund's program subtree when both set, subtree via recursive CTE in `NOT IN`), Z16 (subsidiary+program trees acyclic AND exactly one root each), Z17 (warning: intercompany accounts net 0 per currency), Z18 (warning: negative restricted-fund balance), Z19 (warning: active R/E leaf with activity lacking an effective 990 code, nearest-ancestor via recursive CTE). **Z8/Z9 DEFERRED to p16.1** (they reference `reconciliations`/`splits.reconciliation_id`, absent until p16.1): kept in the registry as clearly-marked placeholders whose SQL is `sqlDeferred` (`SELECT '' WHERE 0`, always zero rows) + a `// TODO(p16.1)` — so the numbering/registry is stable and p16.1 is a pure fill-in; NO reconciliation checks were invented against absent tables. **Z18 SIGN SENSE (the one recorded design choice):** the task's literal "sum of ALL split amounts per (fund,currency)" is **provably vacuous** — Z10 forces every txn to net zero WITHIN each fund group, so the total is ALWAYS 0 on Z10-clean data and could only go negative when Z10 already fired, never independently surfacing the D23 overspend. So Z18 tracks the fund's **cash/asset balance** instead (the task's own clarifying clause, "a restricted-fund cash/asset balance going negative is the flag"): net-debit `SUM(amount) < 0` over the fund's splits **on ASSET accounts**, per currency. Because the whole fund nets to zero, that asset sum equals unexpended restricted resources; it goes negative exactly on "receive 50k then spend 90k" (each txn fund-balanced) — the genuine overspend. Non-expense applications (building purchase, loan principal) move between assets and never false-positive. Proven by a store-built (not tampered) `TestZ18NegativeRestrictedFund` that raises Z18 with **zero Error violations**. **CLI (`cuento check [-db PATH] [--strict]`):** opens the db (reads only), runs `Check`, prints violations `SEVERITY RULE: detail` in a deterministic order (errors first, then rule, then detail) + a summary; exit logic factored into the pure, unit-tested `checkExitCode(vs, strict)`: **1 on any Error always; 1 on any Warning too under --strict; 0 otherwise** (a deliberate non-zero exit rides up via an `exitError{code}` that `main` unwraps with `errors.As`, so violations print without a spurious `log.Fatalf` line). **make check WIRED** (stub replaced): `check: build`, then a FRESH `mktemp` db is migrated and `cuento check`ed — an empty migrated db (seeded roots, no splits) must be CLEAN (exit 0); the temp db is trapped-and-removed (hermetic). `fixtures/sample.db` is p09.3 and gitignored, so the default does NOT depend on it — but if it happens to exist locally, `make check` also runs `cuento check --strict` on it. **Tests-first (observed):** with `Check` temporarily neutered to return nil, all 21 negative/warning tests failed for the right reason ("want rule ZN flagged; got rules []" — detection missing) while the two clean-pass tests passed; restoring `Check` turned every test green. Negative tests corrupt a THROWAWAY copy with raw SQL, dropping the guarding trigger where one blocks the corruption (a `dropTriggers` helper; the task sanctions bypassing triggers on a throwaway db) — Z2/Z14/Z15-presence/Z16-root need a trigger dropped, Z4 uses a bad `transaction_id` on a valid leaf so no trigger fires. **Warning tests build their condition THROUGH the store** (not by tampering) so no Error rule (esp. Z3) cascades — Z17 posts a one-sided intercompany leg, Z18 a store-valid overspend, Z19 a store-valid uncoded expense leaf with a posted txn. No new dependency; `go.mod`/`go.sum` unchanged; `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p08.4** — Balance queries (`internal/store/balances.go` + `internal/db/queries/balances.sql`), the READ-ONLY backbone for registers/fund pages/program pages/reports (Appendix E over these, p15.2). Six store methods: `SubtreeBalancesAsOf`, `PeriodActivity`, `FundBalancesAsOf`, `FunctionalActivity`, `ProgramActivity`, `RegisterPage`. **Scope-closure (D18):** each scoped aggregate closes the subsidiary descendant set with an inline `WITH RECURSIVE scope(id)` (base = the scope sub itself, so a leaf scopes to just itself) and joins `t.subsidiary_id IN (SELECT id FROM scope)`. sqlc v1.31.1 DOES accept a recursive CTE referenced in an outer `WHERE ... IN (subquery)` (verified by `make gen`) — the Go-side per-sub merge fallback was NOT needed (this is a NARROWER limitation than p08.2's "recursive-CTE alias in an outer EXISTS/WHERE" note: an `IN (SELECT id FROM cte)` subquery resolves; a bare CTE-alias column in the outer clause does not — see RegisterPage). **Money stays int64 (rule 3):** sqlc types a bare `SUM()`/`SUM() OVER()` as `sql.NullFloat64` (REAL) — every aggregate is wrapped `CAST(... AS INTEGER)` so the generated column is `int64` and no float ever touches money (SQLite returns the exact integer sum at runtime; the CAST only pins the static type). **Unrestricted fund (D20):** represented as **fund id 0** via `COALESCE(sp.fund_id, 0)` (the generated `FundID` is a non-null `int64`), matching Appendix E's zero-FundID convention that p15.2 builds on. **`FundBalancesAsOf` is ASSET-side only (recorded design choice, Z18 precedent):** a sum over ALL of a fund's splits is IDENTICALLY zero for every (fund, currency) — every txn nets to zero within each fund group (D20/Z10) and scoping/date filter whole transactions, so the total measures nothing. Restricting the join to `accounts.type = 'asset'` yields the fund's cash/asset position = unexpended restricted resources (grant received then partly spent shows the remainder), the same reasoning as p08.3's Z18. `FunctionalActivity`/`ProgramActivity` do NOT have this problem: their `functional_class IS NOT NULL` / `program_id IS NOT NULL` filters keep only the one-sided expense/R-E leg (present iff expense/R-E, D21/D24), dropping the balancing cash counterpart, so the sum is non-zero and meaningful. **`RegisterPage` window + keyset:** the running balance is a SQL window `CAST(SUM(amount) OVER (PARTITION BY currency ORDER BY date, split_id ROWS UNBOUNDED PRECEDING) AS INTEGER)` (per-currency: one account is usually single-currency but FX Clearing is multi). Total order `(date, split_id)` throughout (split id is globally unique + monotonic, so no txn-id tiebreak needed) — window ORDER, final ORDER BY, and cursor tuple all IDENTICAL. **KEYSET PAGING IS APPLIED IN GO, not SQL:** the window must see the WHOLE filtered set before the cursor cuts a page (else page 2's running balance restarts); but sqlc resolves a windowed-CTE's DERIVED columns only in a terminal SELECT/ORDER BY projection — it REJECTS them in an outer `WHERE` (verified: two-CTE and qualified-alias forms both fail `column "split_id" does not exist`). So the query returns the full filtered/windowed/ordered set and the store seeks past the cursor + takes `limit+1` rows in Go (the sanctioned "post-process in Go where sqlc can't express the construct"). The window still runs in SQL over the whole set, so the running balance is exact and continues across pages (tested: page-2 first-row running continues page-1's last, not restart). `RegisterPage(account, cursor, filters, limit)` returns `(page []RegisterRow, next RegisterCursor, hasMore bool, err error)`; `limit <= 0` = single page of everything after the cursor. **Optional filters (date range, free text on txn/split memo + payee, fund, subsidiary, program) via `(? = 0 OR col = ?)` pairs:** each filter binds a 1/0 active flag AND the compared value as TWO positional `?` (the `?N`/`@name` reuse form is mangled by sqlc's sqlite parser, p04.2 quirk), so the store passes each filter value twice (text three times: txn memo, split memo, payee name); an inactive filter short-circuits on `? = 0`. **Tests-first (observed):** with all six method bodies neutered to `return nil, ...`, all 10 balance tests failed for the right reason (assertion failures "cell N/CCY missing" / "page = 0 rows, want K" — the small hand-computed dataset built through the store fine, only the reads returned empty); restoring the bodies turned every test green. The dataset is a root + 2 child subs, 2 asset + 1 revenue + 1 expense account, a grant fund + unrestricted, root + educ programs, 6 non-deleted transactions across dates/subs/funds/programs/currencies (USD + MXN, one multi-currency FX-clearing account) plus a **7th soft-deleted** transaction asserted excluded from every method. Reused accounts.go's `nullInt64Ptr`/`nullStringToPtr` and added the int64 inverse `nullInt64ToPtr`. `balances.sql` pure ASCII (p04.2). No new dependency; `go.mod`/`go.sum` unchanged; `make gen` reproducible, `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p08.5** — Account merge (`internal/store/merge.go` + two queries in `transactions.sql`). `MergeAccount(ctx, src, dst)` folds a source leaf into a destination leaf under **ONE** `write()` change (one `changes` row anchors every version row — `TestMergeRepointsSplitsAndRecons` asserts change count +1). **Validations (all on the tx-bound `q`, p08.2 TOCTOU discipline; typed sentinels):** `src == dst` → `ErrMergeSelf` (checked before the tx); src is a leaf (`AccountIsLeaf`) → else `ErrMergeNotLeaf`; dst is a leaf → else `ErrMergeIntoPlaceholder` (two distinct errors for the two accounts); dst active → else `ErrMergeIntoInactive`; **STRICT `src.Type == dst.Type`** → else `ErrMergeCrossTypeClass`; dst's subsidiary set ⊇ src's (via `subSet`, mirroring `validateMove`) → else `ErrMergeSubsetSubs`. **"same type" vs "type-class" reconciliation (the recorded choice):** the step body says "same type", PLAN p08.5 says "same type-class" — they diverge only for revenue↔expense (which `typeCompatible` treats as compatible). Merge uses **strict type equality**, NOT `typeCompatible`, because splits are repointed **snapshot-from-live with no re-defaulting**: a revenue split (functional_class NULL, program set) moved onto an expense account, or an expense split onto a revenue account, is an **invalid split** — `trg_splits_function_matches_type_update` / `trg_splits_program_matches_type_update` fire on the repoint UPDATE (verified they exist BEFORE UPDATE in 00010) and Z14/Z15 would flag it. Strict equality is both the safe reading and the correct one; no listed success test needs a cross-type merge. **Repoint mechanics:** `SplitIdsByAccount(src)` captures the split ids **FIRST** (before any write — after the first repoint a `WHERE account_id` lookup would confuse moved splits with dst's pre-existing ones); then per id `RepointSplitAccount(dst, id)` (live `UPDATE splits SET account_id`) followed by `insertSplitVersion(..., "update", id)` — snapshot-from-live AFTER the update, so the new op='update' snapshot records `account_id = dst`. `SplitIdsByAccount` is **NOT** filtered by `transactions.deleted` ("every splits row" moves, so src ends fully clear and Z2 sees no split stranded on the deactivated source — a soft-deleted txn's split moves too). Only src's captured splits get op='update' rows; **dst's existing splits are never versioned**. **Append-only audit (rule 5/14, D4):** the pre-merge op='create' split versions (account_id=src) are left UNTOUCHED — that is what keeps as-of history intact (`TestMergeHistoryIntact`: `TransactionAsOf` at a time BEFORE the merge still shows the split on src, after shows dst; ordered create@T0 < merge via an injected `WithClock`). **Deactivate src** inline (`active=0`, op='update', every other column carried through) — NOT via the public `DeactivateAccount` (which would open its own change), so it rides the SAME merge change. **dst is NEVER written** — its functional default / default program / 990 code are trivially kept (`TestMergeFunctionDefaultKept` compares the full dst row before/after). **Reconciliations repointing is DEFERRED to p16.1** — the `reconciliations` table and `splits.reconciliation_id` do not exist yet (p16.1); a `// TODO(p16.1): repoint reconciliations from src to dst` marks exactly where it belongs (right after the split repoint), the merge doc-comment notes it, and `TestMergeRepointsSplitsAndRecons` carries the same TODO — it asserts the **splits** repointing now, the recon half is tagged for p16. Each Blocked test satisfies every OTHER constraint so only the target error fires. **Tests-first (observed):** a stub `MergeAccount` returning `ErrMergeNotImplemented` (rather than a no-op, which would falsely pass `TestMergeFunctionDefaultKept`) made all six tests fail on behavior — the Blocked tests on "want ErrMergeX; got not-implemented", the others on the merge call erroring; implementing turned all six green. Two new queries (`SplitIdsByAccount :many`, `RepointSplitAccount :exec`) pure ASCII, positional `?`; `make gen` reproducible. No new dependency; `go.mod`/`go.sum` unchanged; `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p09.1** — The canonical synthetic fixture (Appendix D), the one dataset CI/goldens/integrity tests use (real data never enters, rule 11). **Package placement is a recorded deviation from the step text.** PLAN says "in `internal/testutil/`", but that is **impossible without breaking store's tests**: `internal/store`'s tests are `package store` (internal) and import `testutil` for `NewDB`/`AssertVersioned`; the moment `testutil` imports `store` (which the fixture needs), the store test binary becomes `store(test) → testutil → store`, an import cycle Go forbids for internal test packages. So the store-importing builder lives in a leaf subpackage **`internal/testutil/fixture`** (`fixture.New(t) *Fixture` with `DB`/`Store`/`IDs`/`Expected`); `internal/testutil` stays store-free. Moving `NewDB`/`AssertVersioned` out to break the cycle the other way would touch every store test — the subpackage is the smallest reversible choice. **Root rename, not a second root (single-root trigger):** `New` renames the seeded root subsidiary (id 1, "Organization") to "Rio Verde Internacional" via `UpdateSubsidiary` (an update version row), then `CreateSubsidiary` adds "RV Estados Unidos" (USD) and "RV Mexico" (MXN); root program "General" (id 1) gains children Educacion + Food Pantry. **23 accounts** (2 placeholder parents Revenue/Expenses + 21 natural leaves), 2 funds (Beca Agua 2025 scoped {MX,US} program Educacion; Building Fund scoped {US}), **29 transactions (28 non-deleted, 63 splits)** 2025-01→2026-06. **The three warning-trap decisions (the fixture must trip EXACTLY Z19):** (1) **Z19 = one leaf.** Event Income is the deliberately-unmapped active R/E leaf — the Revenue placeholder parent carries NO 990 code and Event Income none, so it inherits nothing (Unmapped bucket); its three siblings carry own codes (Contributions VIII.1f, Government Grants VIII.1e, Program Service Fees VIII.2). The Expenses parent DOES carry IX.24e so every expense leaf inherits it (none unmapped), with Bank Fees overriding to IX.11g (the leaf-override for `Effective990Codes`) and Salaries own IX.7, Occupancy own IX.16. Event Income is posted activity (gala tickets) so Z19 fires; asserted by id in the test. (2) **Z18 stays silent:** Z18 is per-(fund,currency) asset-side, so the USD "multi-sub scope" spend in RV Estados Unidos against Beca Agua would drive the fund's USD asset balance negative — a small USD Beca Agua receipt in the US sub (2,000.00) precedes it, keeping the USD asset balance non-negative. (3) **Z17 stays silent / D3 single-currency:** both Due-from/Due-to are USD, so the intercompany pair is modelled entirely in **USD equal-and-opposite** — the MX child leg's USD debit lands on FX Clearing (the only USD-capable MX account besides Due-to), keeping each leg single-currency while the flagged accounts net zero per currency. **Deferrals (tables absent — NOT built):** `exchange_rates` is p14 (every exported aggregate is native-currency, no conversion) and the finalized 2026-05-31 Checking US reconciliation is p16; both have documented **no-op seam methods** (`(*Fixture).ExtendRates`/`ExtendReconciliation`) so p14/p16 slot in without reshaping `New`, and the transactions are designed so the reconciliation can be added later without renumbering (Checking US carries restricted + unrestricted splits and leaves two 2026 uncleared items). **Exported aggregates are an out-of-band hand tally, not query-reversed:** `Expected` (in expected.go) captures per-sub trial-balance-zero-per-currency, specific account balances, fund balances (incl. unrestricted fund id 0), functional-matrix cells, per-program activity, effective-990 rollups (incl. the Unmapped bucket, code ""), and intercompany-net-zero — computed independently from the transaction amounts (a separate tally proving per-fund zero-sum and per-sub trial-balance-zero), so `TestFixtureKnownAggregates` genuinely validates the p08.4 queries rather than comparing a query to itself. **Deterministic monotonic clock** (`store.WithClock` advancing 1s per change from a fixed base) gives every change row a distinct increasing timestamp, so the twice-edited transaction (fund + program changed across edits) has orderable versions for later as-of tests. **Tests-first (observed):** with `build` stubbed to skip the transaction posts, both tests failed on assertion MISMATCH (empty ledger's balances/funds/functional/program cells absent, no Z19 activity) — not undefined symbols; restoring the posts turned both green. `ledger.Check` on the fixture: **0 Error violations, exactly 1 Warning (Z19 naming Event Income)**. No new dependency; `go.mod`/`go.sum` unchanged; `make gen` reproducible, `make lint test check` + govulncheck exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p09.3** — `cmd/ledgerimport` (LOCAL-ONLY, never deployed) converts the cleaned full-ledger export (docs/ledger-export.md) into a cuento db by DRIVING `internal/store` (rule 2/7 -- every produced row is validated by the same invariants; the importer opens no tx and writes no SQL of its own). **Mapping format is CSV + JSON, NOT YAML (the one recorded design substitution):** PLAN/D22/D26 say "mapping.yaml", but rule 1 / D15 forbid a yaml dependency. Two stdlib-parseable files carry the same information: an **account-mapping CSV** (`encoding/csv`) -- one reviewable row per distinct source account (`source_acct, cuento_type, cuento_parent, subsidiaries(;-sep), functional_class_default, default_program, form990_code, name_en, name_es`) -- and a **global config JSON** (`encoding/json`, `DisallowUnknownFields` so a mapping typo fails review loudly) carrying the subsidiary tree (country->{name,base_currency}), program tree (kat->name), fund derivation (donor->{name,funder,purpose,restriction,subsidiaries,program}), functional-class map (kls->program|management|fundraising), base/FX-clearing/opening-balance account names, and row filters (skip_countries, opening_balance_typs). This is a within-allowlist substitution, no `go.mod` change. **Two subcommands, cores factored to take `io.Reader`/`io.Writer`/`*store.Store` so tests drive them with SYNTHETIC content (rule 11 -- the real `fixtures/source/` file is opened only at RUNTIME by the CLI wrapper in `main.go`, never in a test; I never read it):** `runAccounts` collects distinct source accounts (union of every `country` they appear under -> subsidiaries; `stmt` A/L/I/E/O -> type; `parent` -> cuento_parent; `kls`/`kat` best-guesses) and emits the reviewable skeleton; `runBuild` renames the seeded root subsidiary (id 1) + creates children (single-root trigger, never a 2nd root), creates programs under seeded root "General" (id 1), funds, the account tree (topo-sorted parent-before-child), payees, and transactions grouped by `tid`. **Amounts are exact (rule 3, the doc's core hazard #2):** `NetDebit = money.Parse(db) - money.Parse(cr)` over the AUTHORITATIVE `db`/`cr` columns with `money.NumberPlain` (dot-decimal), NEVER `ndb`/`fndb`/`v` (float-subtraction noise) and NEVER `ParseFloat` -- a blank amount side maps to 0 (money.Parse rejects ""); a zero net-debit split is dropped (the `amount <> 0` CHECK). Exponents come from `store.Currencies` (D1), not hardcoded. **FX-clearing decomposition (D3, hazard #1):** a `tid` group is bucketed by split `currency`; each single-currency bucket is posted as its OWN transaction, its residual (splits don't net to zero within the currency) balanced by a counter-leg on the configured **FX Clearing** account so each leg independently sums to zero. Both legs' amounts come from the source **authoritative db/cr** (via `NetDebit`); **`xrt` is NOT used for amounts** -- computing a leg from a 6-dp float rate would reintroduce exactly the float noise rule 3 forbids, so the source's own two currency legs stand as the exact amounts. **Counter-legs are computed PER FUND, not just per currency (the load-bearing correctness point):** the store enforces zero-sum WITHIN EACH FUND GROUP (D20/Z10), so a bucket's residual is grouped by `fund_id` (NULL = the unrestricted group) and one FX-Clearing counter-leg is emitted per fund with a nonzero residual, CARRYING that fund -- a per-currency-only counter-leg would net a restricted fund group nonzero and the store would reject the whole transaction (a cross-currency grant is exactly this case; asserted by a restricted multi-currency tid in `TestImportedBooksBalance`). FX Clearing is equity, so a fund-tagged counter-leg needs no program (A/L/E carry none). A single-currency group posts directly. **Opening balances (D22, hazard #8):** a group whose `typ` is in `opening_balance_typs` routes its residual to the per-subsidiary `Equity:Opening Balances` account. **Non-balancing groups are SURFACED, never force-balanced (hazard #4):** a genuine single-currency imbalance routes the residual to Opening Balances AND appends a `BuildResult.Warnings` line; if the store still rejects a group (`ErrUnbalanced`/`ErrFundUnbalanced`), the import catches it, warns, and CONTINUES -- amounts are never nudged onto an existing split. `main.go`'s `build` runs `ledger.Check` on the produced db and exits non-zero on any Error (the D26 rehearsal gate). **Payee source is configurable (`Config.PayeeColumn`, default "" = no payees):** the task's "payee-ish column as configured" -- `desc` (the multi-line memo) would mint thousands of junk payees on real data, so which column feeds the payee is a p09.4 tuning knob; memos always come from `desc`. **`--anonymize`** hashes payees + memos (sha256 first-8-bytes hex) so a shareable sample db carries no real names/notes. **`make fixture`** wired: builds `bin/ledgerimport`, `rm`s any stale `fixtures/sample.db`, runs `build` against the gitignored `fixtures/source/{jrnl.csv,mapping-accounts.csv,mapping.json}` with `--anonymize` (paths overridable) -- LOCAL-ONLY, never in `make test`/CI, the p09.4 human-run step. **ALL tests use SYNTHETIC data (rule 11):** parser tests feed invented 22-field lines (quoted multi-line memos, dot-decimal amounts, garbage-filled float-noisy net columns asserted IGNORED, a multi-currency tid); `TestMappingAppliesSubFundProgramFunction` builds a tiny synthetic org and asserts sub/fund/program/functional defaults applied; `TestImportedBooksBalance` builds into a temp db and asserts `ledger.Check` has ZERO Error violations, the multi-currency tid produced TWO FX-clearing-paired txns, Opening Balances absorbed the opening group, and the consolidation-marker row was skipped; a `--anonymize` test asserts no produced payee/memo equals a synthetic original. Tests-first (observed): with `runAccounts`/`runBuild`/`BuildResult` undefined the package failed to compile ("undefined: runBuild" etc.) -- the right reason -- then implementing turned all 8 green. No new dependency; `go.mod`/`go.sum` unchanged (`make gen` reproducible); `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).
- **p09.4** — Go-live import rehearsal with a **best-guess autonomous mapping** (human authorized); the reviewed mapping, `cuento check --strict`, and the spreadsheet spot-check remain human gates before cutover (see `docs/golive.md`). **Result: `cuento check -db fixtures/sample.db` = 0 Errors, 151 Warnings (all Z19 unmapped-990)** — the non-strict go-live goal. Build aggregates: 3 subsidiaries / 1 program (root "General") / 0 funds / 293 accounts (232 leaves + 59 parent placeholders + 2 synthetic import accounts); ~42.1k non-consolidation source `tid` groups → ~49.5k cuento transactions (~34.6k single-currency direct + ~7.6k multi-currency decomposed through FX Clearing into paired single-currency txns, D3); ~1.0k non-balancing single-currency groups routed their residual to per-subsidiary Opening Balances as counted warnings (hazard #4, never force-balanced); consolidation-marker rows skipped. **Two committed code changes, both generic + synthetic-test-covered (rule 11):** (1) **HNL currency seed** — the source's local currency (Honduran Lempira, ISO 4217 exponent 2) was absent from the p03.1 seed; a new forward migration `00011_currency_hnl.sql` seeds it (rule 4: never edit 00003; forward-only, no down), without which the HNL subsidiary's `base_currency` FK and every HNL split fail. `TestSeedCurrencies` extended to assert HNL present. (2) **Source dimensions gated by account type** — `resolveSplit` forwarded the source `kls` (functional class) to EVERY split and `kat` (program) whenever mapped, but the export populates `kls` on non-expense lines (~72k) and `kat` on non-R/E lines (~52k); the store rejects a functional class off an expense account (`ErrNonExpenseFunction`) and a program on a balance-sheet split (`ErrProgramOnBalanceSheet`), which was dropping ~43k transactions. Fix: the builder records each created account's cuento type (`builder.acctType`), and `resolveSplit` applies `kls`→functional class ONLY on expense splits and `kat`→program ONLY on R/E splits; the store's own account-default path fills the rest. Covered by `TestSourceDimensionsGatedByAccountType` (a synthetic revenue+asset pair that both carry `kls` and `kat` and must post with NULL functional_class / NULL program). **Best-guess mapping decisions (flagged for human review in docs/golive.md, values kept in the gitignored `fixtures/source/mapping-notes.md`):** subsidiary names (placeholders); functional-class 4→3 enum mapping (D21); **program handling = simplest** (all R/E on root "General", no `kat`→program mapping — richer option left for the human, D24); **funds = none** (all unrestricted, donors not yet named, D20); **990 codes = none** (→ the 151 Z19 warnings, D25); **account tree** = 59 source parent grouping paths materialized as type-pure placeholder accounts with subsidiary set = union of children (superset invariant Z12), one CONS-only leaf mapped to root as a fallback (no splits); consolidation rows skipped; imbalance→Opening Balances; no payees. **Real data never entered the repo (rule 11):** `fixtures/source/{jrnl.csv,mapping.json,mapping-accounts.csv,mapping-notes.md}` and `fixtures/sample.db` are all gitignored (verified `git check-ignore`); only structural descriptions and aggregate counts appear in committed docs. `make check` passes on the CI path (no `fixtures/sample.db` present); locally with the sample db present it additionally runs `--strict`, which by design fails on the 151 Z19 warnings (the human-review gate). `make lint test`, `go vet`, `govulncheck ./...` exit 0; `go.mod`/`go.sum` unchanged (no new dependency).
- **p10.2** — Base layout + theme + i18n wiring + CSS foundation. **`base.tmpl` is the shared shell**, split into reusable partials (`shell-open`/`shell-close` frame the `<html>…<main>` document; `shell-nav`, `theme-control`). html/template shares ONE parse set, so a page cannot override a shared "content" block (a redefinition error at parse); instead each shell page (`base.tmpl` landing, `settings.tmpl`, `styleguide.tmpl`, `admin.tmpl`) is a self-contained top-level template that emits `shell-open`, its own `<main>` body, then `shell-close`. A tiny `shellTitle` template func pairs the `shellPage` model (`.Shell` chrome + `.Page`) with a per-page localized head title so the frame stays shared while each page's `<title>` is a real `{{t}}` string. **Theme is SSR / flash-free:** `resolveTheme` reads the `cuento_theme` cookie first (set the instant the user toggles), else the logged-in user's persisted `theme` setting, else `"auto"` — all server-side, so `<html data-theme=…>` is correct in the first byte with no client round-trip and no flash. An unrecognized cookie value is ignored (falls through), so a tampered cookie can't inject arbitrary attribute text. `CurrentUser` gained a `Theme` field (via `UserByID`) for the logged-in-no-cookie fallback. **`POST /theme` (AnyUser):** validates the value against `store.ValidTheme` (light/dark/auto), sets the cookie (HttpOnly — no JS reads it, SSR only; `Secure` off only in `-dev`; SameSite=Lax; 1-year), and for a logged-in user persists `SetUserTheme` through the write funnel (one audited change, `theme` IS in the users_versions snapshot). PRG: 303 back to a **same-origin** Referer (`sameOriginPath` blocks open-redirects — leading single "/", no scheme/host, not "//"), else "/". **Data-driven perm-gated nav:** `navSections()` is an ordered table of `{LabelKey, Href, Perm}` for the Appendix-F top-level sections; `visibleNav` renders an entry only when its route is **registered** (`registeredGetPaths()` derived from `routes()` — no dead links) AND the user **satisfies** its Perm (reusing the p06.3 `decide` policy so nav visibility and route enforcement can never disagree; the `ReportGroup` reports entry means "any grant or admin" for nav, the concrete report routes gate per group in p15). p11–p18 append their nav entry when they register their route — the entry lights up automatically, no nav edit. **Recorded scope choice — a minimal `GET /admin` stub (Admin):** the DoD requires proving "Admin sees the admin entry, a non-admin does not", but the no-dead-links rule hides the admin section until `/admin` exists. Registering a minimal Admin-only landing stub (parallel to the required `/settings` stub — both render through the shell, are localized, and give the nav a real permitted target) is the smallest reversible way to make the admin gate real and testable now; the real admin pages (users, subsidiaries, currencies, ops) still land in p11.3/p13.2/p14.2/p18.3. So this step registers **four** routes, not three: `POST /theme`, `GET /settings`, `GET /admin` (all in the matrix), plus `GET /styleguide` **only in `-dev`** (absent from the registry — and thus 404 — in production; Public so a designer views it login-free). **Login language switcher:** plain `<a ?lang=…>` links (no JS, CSP-safe); the lang middleware already honors `?lang` (D14), each language labelled in its OWN tongue (`lang.<code>` identical across catalogs). **CSS foundation (`static/app.css`, hand-written, strict-CSP: styles live only in the file, never inline):** three layers — TOKENS (CSS custom properties), RESET, COMPONENT primitives. Light/dark via `color-scheme` + `light-dark()`: every theme token is a `light-dark(light, dark)` pair and `color-scheme` (set per `data-theme` on `<html>`: `auto`→`light dark`, `light`/`dark`→the fixed side) selects the active side — no media query, no JS. **No hand-written JS this step** (the theme toggle is a plain `<form>` POST), so no `node --test` unit was needed and the boring-frontend/CSP surface stays minimal. **e2e:** `e2e/tests/shell.spec.js` logs in via the harness fixture, asserts the shell landmarks + a localized nav label, toggles the theme and asserts `data-theme` changes and **persists across a reload** (SSR from the cookie), and checks the login page localizes to Spanish via the switcher; `make e2e` green (6 specs: 3 login + 3 shell). **Tests-first (observed):** the new `shell_test.go` failed to compile — `undefined: themeCookieName` and the not-yet-registered `/theme`/`/settings`/`/styleguide` routes / missing `base.tmpl` — the right reason; implementing turned every listed test green (`TestThemeCookieSSR`, `TestNavLocalized`, `TestNavPermGated`, `TestHTMLLangMatchesLocale`, `TestSettingsStubRendersShell`, `TestThemePersistsCookieAndSetting`, `TestStyleguideDevOnly`), and the pre-existing `TestCatalogParity`, permission-matrix, and asset/login tests still pass (the new routes flow through the matrix automatically). No new dependency; `go.mod`/`go.sum` unchanged; `make gen` reproducible (one query `SetUserTheme` + `UserByID` gains `theme`); `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `make e2e` green.
- **p10.3** — htmx wiring + the reusable form-error convention every later form (accounts p11, transactions p12, reconciliation p16, import p17) inherits. **Vendored pinned htmx 2.0.4** (`internal/web/static/htmx.min.js`, SHA-256 `e209dda5c8235479f3166defc7750e1dbcd5a5c1808b7792fc2e6733768fb447`, 50917 bytes) — fetched ONCE to vendor, verified byte-identical across two independent CDNs (jsdelivr, unpkg) before committing; served locally through the p10.1 asset pipeline under the strict `script-src 'self'` CSP (rule 12, NO CDN at runtime, tests never hit the network). The version+hash live in `internal/web/VENDOR.md`, kept OUTSIDE `static/` deliberately so `buildAssetManifest` (which hashes every file under `static/`) does not serve/hash the note. Loaded in `base.tmpl`'s `<head>` via `<script src="{{asset "htmx.min.js"}}" defer>` (hashed/immutable in prod, unhashed in `-dev`); **not a Go dependency** (a static asset), `go.mod`/`go.sum` untouched. **Two htmx-config settings, set CSP-safely via a `<meta name="htmx-config">` (never inline script) BEFORE htmx loads:** (1) **`responseHandling` adds `{"code":"422","swap":true}` before the `[45]..` error rule** — htmx 2.x does NOT swap 4xx/5xx by default (it fires `htmx:responseError`), but the form-error convention returns **422** and MUST swap the form-region partial, so 422 is whitelisted for swap (first match wins; 2xx/3xx keep default swap, 204 no-swap, other 4xx/5xx stay error). Without this the Go 422 test passes but the e2e swap silently fails — so the **e2e is the load-bearing proof**, not the Go test. (2) **`includeIndicatorStyles:false`** — htmx otherwise injects an inline `<style>` on load, which `style-src 'self'` (no unsafe-inline) would block with a console CSP error on every page; any indicator CSS goes in `app.css`. **`hx-boost` scope = the top-level nav ONLY** (`<nav class="app-nav" hx-boost="true">` in `shell-nav`): progressive enhancement — the `<a href>` nav links still do full-page navigation without JS; with htmx they become targeted body swaps. Boost is NOT on `<header>`, the logout form, the theme form, or page content (task: nav only). **Form-error convention (the reusable pattern):** a form's markup is a **SINGLE-SOURCED named partial** (demonstrator: `"demo-form"` in `templates/formdemo.tmpl`, `hx-post`+`hx-target="#demo-form"`+`hx-swap="outerHTML"`). The full page renders it inside the shell (`{{template "demo-form" .Page}}`); a failed POST re-renders **JUST that partial** — so input ids (`demo-name`/`demo-email`) stay **stable across the swap** (anti-jank, Appendix C) with zero duplicated markup. On invalid input the handler returns **HTTP 422** via the thin reusable helper **`renderFormError(w, r, partial, model)`** (a 422 + partial-name + model over the existing `render`, which already sets no-store and rebinds `{{t}}`/`{{asset}}` for the request language). **Field errors are i18n KEYS** (rule 9: Go returns keys) held in an **ordered `formErrors`** value (`.add(field, key)` in field order); the template renders each via `{{t .}}` and stamps **`autofocus` on `FirstInvalid()` ONLY** — the first (top-most) invalid field — so focus lands on the first problem. Reusable field validators return keys: `requiredKey`/`emailKey` → `error.required`/`error.email` (both catalogs). **Demonstrator = a POST on the `-dev`-only `/styleguide`** (Public): a genuine handler (`styleguideSubmit`) going through the real registry + middleware, validating Name (required) + Email (required + plausible), returning **422 + the partial** on invalid input and **200 + the partial with a success message** on valid input. Chosen `-dev`-only so no demo endpoint exists in production and it never enters the permission matrix (parallels the p10.2 `-dev` styleguide GET); a permanent AnyUser POST was equally sanctioned. **`formfocus.js`** (small hand-written ES module, external so it loads under `script-src 'self'` — NO inline handler): browsers don't fire `autofocus` on nodes inserted by a swap, so on `htmx:afterSwap` it focuses the first `[autofocus]` in the swapped region; the pure helper (`firstAutofocus`/`focusFirstAutofocus`) is **unit-tested with `node --test`** (`formfocus.test.js`, 6 cases, stub nodes, no jsdom/no dep), the browser glue a thin guarded shim. **Makefile `test`** now passes `node --test` the `*.test.js` **globs** (not the bare directory — recent Node treats a directory arg as a module to run), so the JS unit suite is discovered. **Tests-first (observed):** `TestFormErrorPartial` first failed with `POST /styleguide status = 405` (the demonstrator route/handler didn't exist yet — GET-only) — the right reason; implementing turned it green (422 = fragment not full page, localized en+es errors resolved via catalog not raw keys, exactly one `autofocus` on the first invalid field, valid POST → 200 not 422). `TestCatalogParity` stays green with the new keys in both catalogs. **e2e** `e2e/tests/formerror.spec.js` (2 specs) drives the real `/styleguide`: an invalid submit swaps the 422 partial IN PLACE (a `window` sentinel survives → **no full reload**), the localized field error is visible, focus lands on the first invalid field; a valid submit swaps in the success message. `make e2e` green (8 specs: 3 login + 3 shell + 2 form-error).  **`*.test.js` excluded from the served assets:** the JS unit test rides along in `//go:embed static` (embed can't filter by suffix), so `buildAssetManifest` and `staticHandler` both skip `*.test.js` (via `isTestAsset`) — test code is never hashed, manifested, or reachable on the Public `/static` URL (rule 12; `TestJSUnitTestNotServed` locks it in, `TestVendoredHtmxServed` proves the real htmx asset serves). `make gen` reproducible (no query change); `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `go.mod`/`go.sum` unchanged.
- **p11.1** — Chart of accounts (`/accounts`), the first big feature page + the template every later CRUD page follows. **Store is the sole validator (rule 8/2):** the handler never re-checks tree/990/sub rules — it parses the form, calls `CreateAccount`/`UpdateAccount`/`SetAccountName`/`SetAccountSubsidiaries`/`DeactivateAccount`, and on a TYPED store error `accountErrorField` maps it to a `(form field, i18n key)` pair, then `renderFormError` re-renders the `account-form` partial at **422** with `autofocus` on that field (the p10.3 convention, REUSED not re-implemented). Every typed error is mapped to an `error.account.*` key in both catalogs (`ErrNameRequired`→name_en, `ErrNoSubsidiary`→subs, `ErrCrossTypeClass`/`ErrCycle`/`ErrSubMismatch`/`ErrAccountNotFound`→parent_id, `Err990TypeMismatch`→form990_code, `ErrFunctionalClassNotExpense`→functional_class, `ErrDefaultProgramNotRE/Missing/Inactive`→default_program, `ErrSubInUseByChild`→subs). **Filtered options live in the STORE, reusing the write-path predicates** (so offered options and accepted writes can never disagree): `ParentOptions(lang,type,excludeID)` returns tree-ordered accounts EXCLUDING self+descendants (via `AccountDescendants`) AND type-incompatible parents (via the same `typeCompatible` the move validates with); `Form990LinesForType(type)` filters the seeded lines by CSV membership reusing `csvContains` (extracted from `check990Type` so ONE predicate serves both) + a new `ListForm990Lines` sqlc query; the subsidiary filter is just `Tree(lang,&sub)`. `TestParentOptionsExcludeDescendantsAndWrongClass`/`TestForm990OptionsFilteredByType`/`TestSubsidiaryFilter` assert on these DATA STRUCTURES, not scraped HTML. **Balances match p08.4 exactly:** `balancesByAccount(ctx,st,asof,scopeSub)` is exposed as a plain function (asof/scope explicit) so `TestBalancesColumnMatchesQuery` asserts equality against `SubtreeBalancesAsOf(sameDate,scope)` with no `time.Now`/HTML dependence; the numbers come STRAIGHT from the query, the function only attaches each currency's exponent for the money formatter. Default (no sub filter) balance scope = the **root subsidiary** (full consolidation, D18), derived from `SubTree()`'s NULL-parent row. **Rule 10 plumbing (the recorded extension):** the money formatters need the user's number/neg/display/date settings, but `CurrentUser` (and `UserByID`) carried only locale/theme — so this step extends the un-pinned `UserByID` projection + `CurrentUser` with `date_format/number_format/display_mode/neg_style` (DB defaults US/signed/minus/ISO apply to any untouched session, so no existing test breaks; the pinned `GetUser` is untouched) and adds `internal/web/format.go` (`formatOptsFor`/`dateFormatFor`, string→`money.FormatOpts`/`DateFormat`) — the single settings→formatter mapping p13.1 reuses. **Move-to-top-level is not offered (recorded limitation):** `UpdateAccount` treats a non-nil `ParentID` as a MOVE target and has no way to express "move to NULL/top-level" (a non-nil 0 resolves to non-existent account id 0 → `ErrAccountNotFound`); so the handler sends `ParentID` only when the selection is **positive** — a positive parent moves, selecting "top level" is a no-op reparent (an account already at top level stays there; re-homing a child up to top level is a store gap left for a later step, not invented here). **Edit is multiple store calls, not atomic across them:** `UpdateAccount` (move/flags/990/program, the most-likely-to-fail) runs FIRST so its typed error maps cleanly, then `SetAccountName`×2, then `SetAccountSubsidiaries` (which propagates the assigned subs UP the ancestor chain, D18 — surfaced in the form via a hint string and proven by `TestSubAssignmentPropagation`); create IS atomic (one `CreateAccount` change). **htmx success = HX-Redirect (the load-bearing UX fix):** the inline form posts with `hx-target="#account-form" hx-swap="outerHTML"`, so a plain 303 would swap the whole `/accounts` page INTO the small form region; `redirectAfterForm` instead sets **`HX-Redirect`** (204) on an htmx request so htmx does a full-page navigation reloading the tree, and falls back to a 303 PRG when JS is off. **Type-change value preservation:** the type `<select>` re-fetches the form (`hx-get` + `hx-include="closest form"`) so the type-dependent regions (functional class shown only on expense, default program only on R/E, filtered 990/parent options) refresh; `overlayFormValues` overlays the in-flight query params onto the rebuilt form so switching type never wipes what the user typed. **Only send type-gated fields:** functional class is sent only when type=expense, default program only when type∈{revenue,expense} — else the store's early `ErrFunctionalClassNotExpense`/`ErrDefaultProgramNotRE` would fire on the happy path. **CSP-clean (rule 12):** tree indentation is `acct-depth-N` CSS classes (NOT an inline `style` attribute, which `style-src 'self'` blocks); Cancel is a plain `<a href>` (NOT an `hx-on:click` inline handler); a `strs` template func supplies the static type/functional-class enums so the value lists stay out of the page model. **Routes registered (rule 8, matrix picks them up):** `GET /accounts` (TxnRead), `GET /accounts/new`, `GET /accounts/{id}/edit`, `POST /accounts`, `POST /accounts/{id}`, `POST /accounts/{id}/deactivate` (all TxnWrite). The `nav.accounts` entry (already seeded in `shell.go`) lights up now that GET `/accounts` exists — no nav edit needed. `newMatrixApp` (test harness) now seeds one account so the `{id}`-parameterized routes resolve to a real resource under the reachability check (the harness's stated forward-compatibility intent for p12's register/history). **Edit-error re-render threads the id (a caught defect):** `parseAccountForm(r, id)` takes the edit id (0 = create) and passes it to `buildAccountForm`, so a 422 re-render of an EDIT rebuilds the parent select with self+descendants excluded and shows the inherited 990 code — a first cut hardcoded id=0 there, which would have offered the account itself as its own parent on an error re-render (invisible to the happy-path tests); `TestAccountsEditErrorReRenderExcludesSelf` locks it in (verified failing when reverted to id=0). **Tests-first (observed):** the two new store methods neutered to `return nil, nil` made `TestParentOptionsExcludeDescendantsAndWrongClass` (new-revenue options empty) and `TestForm990OptionsFilteredByType` (every type "returned no lines") fail on assertions — the right reason (`TestSubsidiaryFilter` is green throughout since it reuses the existing `Tree`, an inherent not-red case, noted here per AGENTS rule 1); the web handler tests failed to compile (`undefined: balancesByAccount`) then 404'd until the routes registered, then went green. **e2e** `e2e/tests/accounts.spec.js` (3 specs): admin logs in, creates an account through the REAL inline htmx form and asserts it in the tree, edits it, and a blank-name submit shows the localized inline field error (form-error convention). `make e2e` green (11 specs). `TestCatalogParity` stays green with the new `accounts.*`/`account.type.*`/`functional.*`/`error.account.*` keys. `make gen` reproducible (two queries added: `ListForm990Lines`, `UserByID` projection); `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `go.mod`/`go.sum` unchanged.
- **p11.2** — Merge UI (`/accounts/merge`, TxnWrite), plugged into the p11.1 accounts page as a second `#account-form` trigger ("Merge accounts") beside "New account". **Two-step, confirm-required flow:** `GET /accounts/merge` renders the `merge-form` partial (source + destination pickers listing only ACTIVE LEAF accounts, since `MergeAccount` requires both to be leaves, D11); `POST /accounts/merge` WITHOUT a `confirm` flag renders the `merge-preview` partial — a consequences summary (how many transaction lines repoint, `0` reconciliations move, the source is deactivated) plus a Confirm control — and performs **zero writes**; a second POST WITH `confirm=1` executes `store.MergeAccount`. `TestMergeConfirmRequired` proves the no-confirm branch does not repoint/deactivate. **Validate at CONFIRM, never a dry-run (the recorded design choice):** there is no rollback-only path and duplicating the store's checks would violate rule 8/the p11.1 "store is the sole validator" convention, so the merge's legality (cross-type, sub-coverage, leaf/placeholder/inactive/self) surfaces on the confirm POST — `MergeAccount` validates-then-writes atomically, so a typed error means nothing was written and "no execution" holds trivially; the handler maps each sentinel to an i18n key and re-renders `merge-form` at **422** (the p10.3 convention, REUSED). The preview still RESTATES the coverage rule as an informational note (`merge.coverage_note`), separate from the error message. **All SIX sentinels mapped** (`mergeErrorField`, mirroring `accountErrorField`) so no legitimate rejection falls through to the 500 path: `ErrMergeSelf`/`ErrMergeNotLeaf`/`ErrMergeCrossTypeClass`/`ErrMergeSubsetSubs`→src select, `ErrMergeIntoPlaceholder`/`ErrMergeIntoInactive`→dst select; a missing selection is a client-side `error.merge.select_both` on src. **Preview count == merge effect BY CONSTRUCTION:** new read helper `store.SplitIDsForAccount` calls the SAME `SplitIdsByAccount` query `MergeAccount` repoints from (NOT deleted-filtered — it moves soft-deleted-txn splits too, per that query's comment), so the previewed split count can never drift from what actually moves; `len()` is the count. **Routes (rule 8, matrix auto-covers):** `GET`+`POST /accounts/merge` (both TxnWrite); the literal `/accounts/merge` is more specific than `/accounts/{id}`, so the Go 1.22+ mux routes it precisely with no conflict/panic (mount + matrix tests confirm). `TestMergePermissions` keeps the explicit ReadOnly-403 / anon-302 assertions the step lists (partly redundant with the matrix). **Tests-first (observed):** the handler tests first failed to COMPILE (`st.SplitIDsForAccount undefined`) — the right reason (the read helper, route, handler and partials all did not exist yet); implementing the two-step handler + partials + route turned all six green (happy path repoints+deactivates via the store; confirm-required does nothing; consequences show the split count + 0-recon note; sub-coverage and cross-type return 422 with the localized message and no execution). **e2e** `e2e/tests/merge.spec.js` creates two expense leaves through the real UI, reviews the consequences, confirms, and asserts the source drops out of the active-only tree while the destination survives. `make e2e` green (12 specs). `TestCatalogParity` stays green with the new `accounts.merge`/`merge.*`/`error.merge.*` keys in both catalogs. `make gen` reproducible (**no query change** — the read helper reuses the existing `SplitIdsByAccount`); `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `go.mod`/`go.sum` unchanged.

- **p11.3** — `/admin/subsidiaries` (Perm **Admin**, Appendix B/F) is the p11.1 accounts page recut for a currency-carrying single-root tree: `subsidiaries.go` renders the depth-indented `store.SubTree` list (name / base currency / active) with an inline htmx create/edit form (name, parent, base currency) and a deactivate action; `subsidiaries.tmpl` + `subsidiary_form.tmpl` mirror `accounts.tmpl`/`account_form.tmpl`. **No new store/SQL** — every method (`SubTree`, `GetSubsidiary`, `Descendants`, `CreateSubsidiary`, `UpdateSubsidiary`, `DeactivateSubsidiary`, `Currencies`) already existed, so `make gen` is a no-op and `go.mod` is unchanged. The handler **never re-validates** (rule: store owns validation); it maps the store's typed errors to i18n keys: create-with-no-parent → `ErrSecondRoot`→`error.subsidiary.second_root`, giving the root a parent → `ErrRootImmovable`→`error.subsidiary.root_immovable`, a descendant move → `ErrCycle`→`error.subsidiary.cycle` (**`ErrCycle` is the SHARED sentinel** accounts moves also return — same physical error, subsidiary-specific key), missing parent → `ErrParentMissing`→`error.subsidiary.parent_missing`; all re-render the form partial at 422. **Deactivate deviates from accounts' deactivate** (which 500s on unknown / 404s on missing): a blocked deactivate (`ErrHasActiveChildren`) re-renders the LIST at 422 with a page-level `error.subsidiary.has_active_children` alert, and the store's no-trace discipline leaves the subsidiary active (the test asserts both). The parent select excludes the subject + `Descendants(id)` and is omitted for the root (presentation only; the store still enforces). The `/admin` landing (`admin.tmpl`) links the page; the six routes go in `routes.go` under Admin so the permission matrix picks them up (matrix hits `/admin/subsidiaries/1/...` on the childless root, so every path is non-404/non-403 for Admin). New `subsidiaries.*`/`error.subsidiary.*` keys added to BOTH catalogs (`TestCatalogParity` green). **Login rate-limiter bypassed in `-dev`** (`login.go`): the limiter (burst 5 per ip+user) exists to choke online password guessing in production; `-dev` is a local, non-adversarial mode (it already relaxes the session-cookie Secure flag for the same reason), and the worker-scoped Playwright suite drives many rapid same-user logins against one `-dev` server that the burst would otherwise throttle into intermittent login-bounce failures (a pre-existing fragility this step exposed). Guarded strictly on `cfg.Dev`; `TestLoginRateLimited` runs `Dev=false` and stays green, so the production guarantee is untouched. `make e2e` green (`e2e/tests/subsidiaries.spec.js`: one login, then create-child → rename → blocked-deactivate). `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `go.mod`/`go.sum` unchanged.
- **p11.4** — Org settings & languages. **`org_settings(key TEXT PRIMARY KEY, value TEXT NOT NULL)`** (migration `00012`) is a simple NON-versioned CONFIG table — absent from Appendix A's versions list, exactly like `currencies`/`report_groups`: no `org_settings_versions`, no trigger, no `changes` wiring. Seeded `org_name` (empty) and `enabled_languages` (`en,es`). Store helpers `OrgSetting(key,def)`/`SetOrgSetting(key,value)`/`EnabledLanguages()` are plain sqlc reads + an idempotent upsert **outside the write funnel** (rule 2 permits config upserts via sqlc without an actor/changes row) — the `/admin/org` handler therefore does NOT use `actorCtx`: these are configuration writes, not audited business mutations. **Report base currency is intentionally NOT an org setting** — it follows the scoped subsidiary's `base_currency` (D18), so it lives on `subsidiaries`, not here. **`en` is always an enabled name language (smallest-choice, recorded):** `EnabledLanguages` guarantees `en` present and FIRST even if an admin drops it from the stored CSV (parse: split on comma, trim, drop blanks, dedupe, prepend `en`) — the required base name (p05.3 fallback) must always have an input; a now-disabled language's stored `account_names` rows are left alone (fallback still uses them). The enabled-languages value is stored VERBATIM and normalized only at read time, so a stray space or dropped `en` never breaks the account form. **Account name inputs are enabled-languages-driven (the p11.4 payoff):** `accountForm.NameEN/NameES` became `Names []nameInput{Lang,Field,ID,Value,Required,LabelArg}` built from `EnabledLanguages()`; the template ranges it, rendering one `name_<lang>` input per enabled language — adding a language in `/admin/org` makes a new name column appear (`TestAccountFormLanguageColumns`), and create/edit write `account_names` for each filled-in enabled language via `SetAccountName` (`TestAccountCreate/EditWritesEnabledLanguageNames`). **Stable field names/ids preserved:** `en`/`es` keep `name_en`/`name_es` and `af-name-en`/`af-name-es` (hyphenated id), so existing `accounts_test.go` and the `e2e/tests/accounts.spec.js`/`merge.spec.js` selectors (`#af-name-en`) stay green. **i18n label for an arbitrary language:** the per-language `accounts.form.name_en`/`name_es` chrome keys were replaced by ONE interpolated `accounts.form.name = "Name (%s)"` passed the uppercased code — any language gets a label without a per-language chrome key, and the UI CHROME stays en/es (`enabled_languages` is NOT wired into `i18n.Langs()`/the shell; chrome falls back to en per D14). New store read `AccountName(id,lang)` returns the EXACT per-language name (no fallback) so the edit form prefills an empty box for a language with no name yet rather than echoing the en name into a foreign input. **Admin org form** (`/admin/org`, GET+POST **Admin**, registered in `routes.go` so the permission matrix auto-covers it; linked from `admin.tmpl`): a small form (org name + enabled-languages CSV) that re-renders with a Saved notice on success (settings pages have no list to PRG to). `TestOrgSettingsPersist`/`TestOrgFormAdminOnly` + the matrix. **Tests-first (observed):** the store tests passed once the helpers existed; the web tests failed behaviorally — `/admin/org` 404 (route absent) and `name_fr` absent from the account form even with `en,es,fr` enabled (form was hardcoded en/es) — then went green after the handler/route/template refactor. `make gen` reproducible (three queries: `GetOrgSetting`/`ListOrgSettings`/`UpsertOrgSetting`); `make lint test check` + `govulncheck ./...` exit 0; `make e2e` green (13 specs); `TestCatalogParity` green with the new `org.*` keys and the single `accounts.form.name` replacing the two `name_en/name_es` keys; `go.mod`/`go.sum` unchanged.

- **p12.2** — Transaction editor (the daily-use grid). The **five traps** are resolved as follows. (1) *Split-id round-trip:* the edit form emits each existing split's id as a hidden `split_id_<i>` field; the handler maps it into `SplitInput.ID`, so `UpdateTransaction` diffs by id — editing one split of a 3-split txn produces exactly one `splits_versions op='update'` and leaves the other two untouched (asserted via a raw `COUNT(*) FROM splits_versions`, not just latest-op). Without it, every save would delete-all + recreate-all (balances still pass, audit wrong). (2) *Pure JS:* amount parse/format, DR/CR↔signed, per-fund imbalance, fund-apply-to-all, and the keyboard-grid state machine live in `txnamount.js`/`txnfund.js`/`txngrid.js` with **no `document` access**, node-tested for real (33 cases). `txneditor.js` is the thin DOM glue (combobox/select-on-focus/date-shortcuts/gating), covered by e2e only. (3) *One DR/CR↔sign site:* the amount column posts ONE **signed** `amount_<i>` field; DR/CR twin columns are a JS convenience that normalizes into it before submit (via `drcrToSigned`), so the server is **mode-agnostic on input** — the both-modes test posts the identical form as a signed-mode and a dr_cr-mode user and gets byte-identical `splits` rows. Signed-mode entry works with no JS (`<form method=post>` degrades). (4) *Stable ids:* every input id is keyed to the row **index/split identity** (`txn-account-<i>`, `txn-splitid-<i>`, …), the SAME key that carries the split-id round-trip — a 422 re-render keeps the same ids (asserted). (5) *Server sole validator, row/totals routing:* the handler calls Post/UpdateTransaction and maps the store's typed errors to slots **without re-deriving validation** — `ErrUnbalanced`/`ErrFundUnbalanced` → the totals bar (`txn-totals-error`); row-scoped errors (`ErrAccountNotInSubsidiary`, `ErrFundProgramScope`, `ErrExpenseNeedsFunction`, `ErrNonExpenseFunction`, `Err*Program*`, inactive/missing) → the offending row (`data-row-error="<i>"`), attributed from data the editor **already loaded** (the sub-filtered account option set gives account-not-in-sub for free; a funded R/E row gives fund-program-scope; structural expense/non-expense gives the class errors). The store returns transaction-scoped errors with no split index, so attribution is a best-effort first-matching-row heuristic over the loaded options — the listed test only requires the error to land on a row, not multi-row disambiguation; an unattributable typed error falls back to the totals bar so it is never silently dropped. Two supporting reads were added (both in scope, no new dependency): `Store.GetTransaction` (live header for edit prefill) and `Store.AccountEditorOptions` (leaf+active accounts in a sub with type/default-program/default-class/subsidiary-set metadata for the combobox + client re-filter). `UserByID`'s projection was extended with `default_subsidiary_id` (mirroring p11.1's money-column extension) and `CurrentUser` gained `DefaultSubsidiaryID *int64` so the editor defaults its header subsidiary to the user's setting, else the sole/root sub. htmx success uses `HX-Redirect` (a full-page nav) so the swap never nests a register inside `#txn-form`; the subsidiary-change re-filter and the 422 error re-render both render the single-sourced `transaction-form` partial (stable ids). Scope boundary held: NO payee suggest/autofill (p12.3), void/duplicate/history (p12.4), or keyboard-only QA (p12.6) — the payee field is a plain combobox over the existing payees list. `make gen` reproducible (the one `UserByID` query change); `make lint test check` + node units + `govulncheck ./...` exit 0; `make e2e` green (16 specs incl. `txn-editor`); `TestCatalogParity` green with the new `txn.*`/`error.txn.*` keys; `go.mod`/`go.sum` unchanged.
  Post-review sharpening: the header **subsidiary change re-filters server-side** (the select's hx-get re-fetches `/transactions/new?subsidiary=…` with the current rows via hx-include; the handler re-filters accounts + funds to the new sub and echoes the typed rows, flagging any row whose account left the sub — never silent-clear; no-JS friendly, the client mirrors it). Edit-prefill amounts use the **user's number format** (rule 10) so a save-without-touching round-trips (an EU-number user's "1.234,56" is emitted and re-parsed identically). The editor **currency defaults to the selected subsidiary's `base_currency`** (D18), so an MXN-subsidiary entry records in MXN rather than a hardcoded USD (the edit form overrides with the txn's stored currency). Two extra e2e specs cover the DR/CR-independent client gating: program/class selects appear only on R/E rows and prefill from the account default.
- **p12.3** — Payee autocomplete + autofill. **Four required tests, split Go/JS:** `TestPayeeSuggestRanking` (Go store) — asserts the ranked order; `TestPayeeTemplatePrefills` + `TestAutofillRespectsSubsidiary` (Go web handler) — assert the rendered partial's fields / dropped rows / notice; `TestAutofillNeverOverwrites` (JS `node --test` on the pure `allRowsEmpty`) — the client guard. **Suggest ranking (`SuggestPayees`):** active payees whose name PREFIX-matches (case-insensitive; `payees.name` is `COLLATE NOCASE`), ordered by the payee's latest NON-DELETED transaction date DESC via `LEFT JOIN transactions … AND deleted=0` grouped per payee; `ORDER BY (MAX(date) IS NULL), MAX(date) DESC, name COLLATE NOCASE, id` so **never-used or only-deleted payees rank LAST deterministically** (chosen default: unused sorts to the tail by name, not interleaved). **Active-only** suggest (a deactivated payee is not offered); recorded here as the smallest choice. **LIKE-metachar handling:** sqlc's sqlite parser REJECTS an explicit `ESCAPE` clause (and SQLite's default LIKE has no escape char), so `likePrefix` in Go neutralizes `% _ \` in the query by mapping each to the single-char wildcard `_` before appending `%` — keeps the match a true prefix (never a mid-string wildcard) for the rare payee name containing a metachar, without an escape clause. **Template (`/payees/{id}/template?sub=`):** finds the payee's last non-deleted txn (`LastTransactionForPayee`, greatest `(date,id)`), then REUSES `SplitsByTransaction` (the existing splits reader) — mapped to editor rows exactly like `txnEditForm` (account/memo/signed-amount-in-user-number-format/fund/program/class), amounts formatted in the **template txn's stored currency** (display-only prefill; the server re-validates on save). **Respects-subsidiary is SERVER-side:** splits whose account is not in the `?sub=` option set (the same `model.Accounts` the combobox offers) are DROPPED; when any drop, an i18n notice (`txn.autofill.dropped_out_of_sub`) is oob-swapped into `#txn-autofill-notice`. **Never-overwrites is CLIENT-side & pure:** `txnpayee.js` `allRowsEmpty(rows)` (no `document`) returns true only when every row has no chosen account, no typed amount/DR/CR, and no memo (a defaulted fund/program/class alone does NOT count as user intent); `txneditor.js` glue applies the fetched rows only when `allRowsEmpty` holds. **Partial reuse (no fork):** a thin `{{define "txn-rows"}}` ranges the SAME single-sourced `txn-row` template; `payee-template` wraps it + the oob notice — an autofilled row is byte-identical to a page-rendered one (stable ids / gating / wiring). **Both routes `TxnWrite`** (they feed the entry flow and exist only to author an entry, matching the editor GET forms); the permission-matrix test picks them up automatically. **Autocomplete UI (boring frontend, strict CSP):** the native `<select id=txn-payee>` stays as the no-JS **id value sink**; the JS reveals a text input (`hx-get="/payees/suggest"`, `input changed delay:150ms` → a suggestions `<ul>`) and hides the select. Picking a suggestion goes through a **delegated** click listener on the stable `#txn-payee-suggestions` container (not an `hx-*` trigger on the freshly-swapped `<li>`) and the template fetch is a **manual `fetch()`** (not htmx) — both dodge the settle-tick wiring race the task warned about. **Create-on-save (recorded interpretation, exceeds the literal build text):** a fresh `-dev` db has no payees and there is NO payee-management UI or seed hook, so the required e2e precondition ("pick a payee WITH a prior txn") can only be built in-browser by letting a saved transaction attach a NEW payee by name. `EnsurePayee(name)` find-or-creates by the NOCASE name in its OWN versioned change BEFORE the txn write (so a later txn-validation failure leaves the payee reusable, not orphaned); the editor posts the typed/picked name via a hidden `payee_name` field, and `txnSubmit` prefers a picked `payee` id else `EnsurePayee(payee_name)`. This is additive/reversible and makes the whole feature usable (an autocomplete over a permanently-empty list would be dead). No payee edit/delete/management page (not this step). **New i18n keys** (`txn.payee_search`, `txn.autofill.dropped_out_of_sub`) in BOTH catalogs — note `txn.payee.search` was renamed to `txn.payee_search` because go-i18n rejects a dotted key that turns an existing leaf (`[txn.payee]`) into a table (reserved/unreserved-key mix). `make gen` reproducible (three read queries added, ASCII, positional `?`); `make lint test check` + node units + `govulncheck ./...` exit 0; e2e green (19 specs incl. the new `payee-autofill`, run ×20 for the htmx-swap paths); `TestCatalogParity` green; `go.mod`/`go.sum` unchanged.
- **p12.4** — Edit / void / duplicate + history panel. **History diff granularity — STRUCTURED in the store, RENDERED in web.** `Store.TransactionHistory(id)` returns typed `HistoryEntry{ActorID, ActorName, At, Op, HeaderDiffs, SplitDiffs}` where each `FieldDiff` carries typed old/new `DiffValue` (`Text` for date/memo/currency/functional-class, `sql.NullInt64 ID` for subsidiary/payee/account/fund/program, `int64 Amount` for money) — NOT formatted strings, because amount formatting is per-user (rule 10) and entity names are language-dependent (rule 9). The web layer (`renderHistory`) resolves ids → names in the request language, formats amounts through the money formatter honoring the user's settings, and maps each `HistoryField` to an i18n label key. This keeps the diff computation unit-testable in Go (assert old=X/new=Y, no HTML). **Diff is computed by walking consecutive snapshots per entity** (header as one entity by `entity_id`; each split by `entity_id`) then grouping the per-row diffs by `change_id` into timeline entries. Two new `:many` queries (`TransactionVersionHistory`, `SplitVersionHistory`) fetch EVERY version row (not the AsOf `LIMIT 1`) JOINed to `changes`+`users` for actor/timestamp, ordered `valid_from, id` so each entity's snapshots are consecutive; grouping re-orders into entries by **change_id ascending** (monotonic with the change timestamp: `changes.id` is autoincrement at write time, so a larger id is a later change -- this is the "ordered by the change timestamp" the audit backbone requires, and it is correct even for a SPLIT-ONLY change with no header version, e.g. an account-merge repoint, which is placed by its own change_id rather than appended after all header changes). **NULL-aware diffing is load-bearing:** reuses the store's `nullInt64Eq`/`nullStringEq` so a fund Unrestricted→set or a functional-class NULL↔set is detected — the exact deltas the step names. A `create` snapshot diffs against the empty side (initial values shown one-sided); a `delete` snapshot one-sided too; an `update` against its predecessor. An update entry's header op is ALWAYS `update` even when only a split changed (matches `UpdateTransaction`'s header-anchor), so the template renders zero header diffs rather than an empty "header changed" block. **CRITICAL void trap:** history loads from the VERSION rows by id (present regardless of the live `deleted` flag), NOT via `GetTransaction` (which returns `ErrTransactionNotFound` for a soft-deleted row) — so the history of a VOIDED txn renders (the one case it most needs); only a txn with ZERO version rows 404s. Edit and duplicate, by contrast, KEEP the live-`GetTransaction` 404 (you don't edit/duplicate a voided txn). **Void = soft-delete, not hard delete.** The route reuses `Store.DeleteTransaction` (rule 14: flip `deleted`, append ONE `transactions_versions op='delete'`, splits left live); a two-step confirm flow mirrors the merge UX — `GET /transactions/{id}/void` renders a read-only consequences review (ZERO writes), `POST` with `confirm=1` executes and redirects to `/accounts` (the txn no longer appears in any register). A `POST` without confirm falls back to the review (defensive). Both routes `TxnWrite`; a re-void of an already-voided id 404s (the review's `GetTransaction` guard). **Duplicate = editor prefilled as a NEW unsaved entry.** `GET /transactions/{id}/duplicate` (`TxnWrite`) reuses the p12.2 editor-model machinery with `TxnID=0`, `IsEdit=false`, and every row's `SplitID=""` (fresh splits inserted under a NEW txn — no diff-by-id), so the form POSTs to `/transactions` (a create). **Payee + memo are COPIED** (a duplicate is usually the same payee/purpose); **the date defaults to TODAY**, not the original's, since a duplicate is a fresh entry authored now (both defensible — the note IS the deliverable). **Timeline timestamp rendering:** rule 10 forbids `time.Format` in the render path and the money package has only a date-only `FormatDate`, so the timeline renders the change's DATE (the `changes.at` RFC3339Nano trimmed to its `YYYY-MM-DD` prefix, then through `FormatDate` in the user's format); the time-of-day is not shown (entry order disambiguates same-day edits) — a deliberate, reversible choice, not a rule-10 violation (parsing the machine timestamp with the string prefix is fine). **Entry points:** the register row (`register.tmpl`) gains an always-present Actions cell with plain full-page `<a>` links (edit / duplicate / void / history) — plain nav keeps the CSP trivial and avoids the htmx-settle dance (only the editor's own JS is reused, unchanged); `regColspan` bumped 7→8 (the register colspan/thead tests picked it up). **Perm enforcement asserted explicitly** (beyond the auto matrix): `TestVoidRequiresConfirmAndTxnWrite` proves a `read` user gets 403 on GET+POST void while the txn stays live, then a `write` user's confirm voids it; `TestHistoryTxnReadMayView` proves `read` may view history. **i18n:** new `history.*`, `void.*`, `register.action.*`, `register.col.actions` keys in BOTH catalogs (`history.split.line` is a `tn` plural); watched the dotted-key/leaf collision (no bare `history` leaf beside `history.*`). `make gen` reproducible (two ASCII positional-`?` `:many` queries added); `make lint test check` + node units + `govulncheck ./...` exit 0; `make e2e` green (22 specs incl. the new `txn-history`, validated ×20 for the swap-touching path); `TestCatalogParity` + the auto permission-matrix green with the four new routes; `go.mod`/`go.sum` unchanged.
- **p12.5** — Funds workspace (`/funds` list + `/funds/{id}` statement + create/edit/close/reopen). **Negative-badge sign rule (Z18):** the list's warning badge is `FundBalancesAsOf(fund) < 0` per currency (net-debit, D2: a fund's ASSET-side sum going negative = overspent restricted resources). This is NOT a fresh computation — it reuses the p08.4 `FundBalancesAsOf` query, which already sums ONLY asset splits (a sum over ALL a fund's splits is identically 0 by fund conservation, D20/Z10). The badge fires if ANY currency is negative (BecaAgua carries both MXN and USD; the fixture deliberately keeps every fund non-negative, so the negative-badge Go test builds an overspent fund inline — receive 500, spend 900, each txn fund-balanced → asset cash −400). **Fund-ledger opening-balance computation:** the statement is a new `FundLedger(fund_id, asof)` query — every non-deleted split tagged the fund whose txn date ≤ asof, across ALL accounts, ordered `(date, split_id)`, with a per-currency RUNNING BALANCE whose window sums ONLY asset splits (`CASE WHEN a.type='asset' THEN sp.amount ELSE 0 END` inside `SUM() OVER (PARTITION BY currency ORDER BY date, id)`). So the running balance tracks the fund's UNEXPENDED (asset-side) position — the SAME quantity the list shows and Z18 warns on — and the CLOSING running balance per currency EQUALS `FundBalancesAsOf(asof)` by construction. **The statement uses the SAME as-of the list uses** (`s.now()`, i.e. today): binding both pages to one as-of makes them agree on a fund's closing balance EVEN under FUTURE-dated splits (a post-dated payment) — without the bound they'd silently disagree, since the list is date-bounded (`FundBalancesAsOf` filters `t.date <= today`) while an unbounded ledger would sum splits dated after today. `TestFundLedgerAsOfMatchesFundBalances` posts a 2099-dated fund split and asserts reconciliation at three as-ofs (past, mid, future) to lock the agreement. **Opening is 0** (the whole set to the as-of is shown from the fund's first split; there is NO period filter — the task doesn't require one, and a fund's splits already live only in its subsidiaries, so no scope filter either). This reconciliation (`closing == FundBalancesAsOf(asof) == list balance`) is the coherence invariant, asserted directly in `TestFundLedger` (store), `TestFundLedgerAsOfMatchesFundBalances` (store, future-dated), and `TestFundStatementOpeningClosing` (web). Non-asset rows (revenue/expense) are STILL SHOWN (per "splits across all accounts") but render "—" in the running column and leave the balance unchanged. **Closed-fund toggle semantics:** `CloseFund` sets `active=0` (p07.3, op=update, audited — never a delete, rule 14); the list splits on `active` via `?closed=1`, showing exactly the active OR the closed set (never both). Close/reopen are inline POST ACTIONS (mirroring `programDeactivate`), NOT a form — the subsidiary checklist + program scope belong to create/edit only. `fundClose` redirects to `/funds?closed=1` (you just closed it, so land where it now lives); `fundReopen` to `/funds`. **Program-scope handling:** the create/edit form has an OPTIONAL program-scope `<select>` (a fund scopes to ≥1 subsidiary via the `sub_<id>` checklist reusing the accounts pattern, D20, and OPTIONALLY a program subtree). On UPDATE, `program_id` is ALWAYS sent as a non-nil pointer (0 = clear, positive = set) using the store's non-nil-0-clears convention; the subsidiary checklist is passed as a NON-NIL slice (`nonNilSubs`) so an empty checklist is treated as "desired = empty" → `ErrFundNoSubsidiary` (rather than nil = "leave unchanged"). **Perms:** fund grants are BOOKKEEPING data (D20, managed like programs), so GET list+statement = TxnRead, create/edit/close/reopen = TxnWrite (subsidiaries/users stay Admin — unchanged). `TestFundsPermReadCannotManage` asserts (beyond the auto matrix) that a `read` user views `/funds` + a statement (200) but is 403 on POST create, POST close, and GET `/funds/new`. **Required assertions, split Go/node:** all funds assertions are Go (store `TestFundLedger`/`TestFundLedgerExcludesOtherFunds`; web list/statement/form/perm) — this step adds NO client logic (the closed toggle is a plain `?closed=1` link; the form uses only htmx attributes), so NO new node-tested module. A new store read `FundSubsidiaryIDs` (mirrors `AccountSubsidiaryIDs`) feeds the list's scope column + the edit form's checklist pre-check. **Matrix seed:** `routes_test.go` now seeds one fund so `/funds/{id}` + `/funds/{id}/edit` resolve for the reachability check (the same pattern the account/txn seeds use). **i18n:** new `funds.*`/`error.fund.*` keys in BOTH catalogs — watched the dotted-key/leaf collision (renamed the statement-button leaf `funds.statement` → `funds.view_statement` because `[funds.statement]` collides with `[funds.statement.title]` etc.). `make gen` reproducible (one ASCII positional-`?` `:many` query added, `FundLedger`); `make lint test check` + node units + `govulncheck ./...` exit 0; `make e2e` green (23 specs incl. the new `funds` spec covering create-with-checklist+program → statement → close → reopen, validated ×20 with 4 parallel workers, no flake); `TestCatalogParity` + the auto permission-matrix green with the eight new routes; `go.mod`/`go.sum` unchanged.
- **p12.6** — Entry-flow hardening + the manual QA script `docs/qa-entry.md` (focus retention, zero layout shift, scroll preservation, keyboard-only 4-split mixed-fund entry, es locale pass). **Automated the two Tests bullets at the handler layer** (no store mocks, real router): `TestTxnStableInputIDsAcrossAllSwaps` extends the existing p12.2 `TestTxnStableInputIDsAcrossRerender` to assert row-0 input ids survive **every** in-flow swap — the subsidiary re-filter `hx-get` swap **and** an htmx 422 re-render, not only the non-htmx 422; `TestTxnInFlowActionsNeverFullReload` proves in-flow editor actions never full-reload mid-entry — the re-filter and 422 swaps return the `#txn-form` **partial** (no doctype/`<html>`/`<nav>`), while a successful save returns **HX-Redirect** to the first split's register (the ONE intentional navigation) and does **not** swap the editor back in. Added a test-only `asHTMXUser` helper (asUser + the `HX-Request` header) + an `isFullPage` body check. **Keyboard-only e2e** (`e2e/tests/entry-keyboard.spec.js`) drives a real 4-split mixed-fund entry with `page.keyboard` events only (Tab/Shift+Tab, Arrow-driven native `<select>` operation via a deterministic `selectByKeyboard` helper — no `selectOption`, the Add-row `<button>` activated by Enter, typed amounts, the date `t` shortcut); the 4 splits balance per-fund (D20: Water Grant +40−40, Unrestricted +10−10) so it is a genuine mixed-fund post. The spec also Tab-walks an asset row's tail (fund → memo) and asserts focus lands on `#txn-memo-0`, **proving** the hidden program/class cells are skipped by native Tab. Stable ×20 solo + ×21 in the full parallel suite (the DoD's ~20× parallel gate), zero flakes. **FINDING (documented + flagged, deliberately NOT fixed here):** `txngrid.js`'s pure `nextCell` grid state machine (Enter-advance, Alt+Arrow row-move, Ctrl/Cmd+Enter save, Escape cancel, Enter-add-row-on-last-cell) is node-tested and **imported** by the DOM glue `txneditor.js` but **never called** — no `keydown` listener consults it — so those keys are inert in the browser today. Keyboard entry is **not broken**: native Tab + native select keyboard ops + the Add-row button cover a full 4-split entry (proven by the spec), and hidden program/class cells (`visibility:hidden`) are correctly skipped by native Tab. Per the p12.6 posture ("do NOT refactor unless it genuinely breaks keyboard entry — flag before a big refactor") this is a **missing ergonomic enhancement**, not a hardening fix: correct wiring would need a skip-hidden traversal (`nextCell` moves col+1 blindly and would land focus on the hidden program/class cells) that does not exist and is untested — i.e. p12.2-scoped feature work left for the orchestrator to green-light. **p12.2 settle-wiring follow-up CONFIRMED:** it does not bite keyboard entry — the account form's self-swapping type select is not in the editor; the editor's only swapped-in `hx-*` triggers are the subsidiary re-filter (set once, before typing rows) and the payee suggest input, and the payee template fetch is fired via a **manual `fetch`** (not an `hx-*` trigger on a freshly-swapped node — `fetchAndApplyTemplate`), so nothing races the settle tick; no account-form refactor. **No i18n/schema/query changes** (all `txn.*`/`error.txn.*` keys already exist in both catalogs). **es locale pass OBSERVED, not inferred:** `TestTxnEditorEsNoRawKeys` sets the user's `locale='es'` (D14: a logged-in user's stored locale is authoritative over `?lang=`, so `?lang=es` does NOT switch an authenticated user's UI — corrected in qa-entry.md) and GETs the editor, asserting the es column headers/title/apply-all button render, that the en title does NOT leak (no fallback-to-en), and that no raw `txn.`/`error.txn.` key substring appears. `make lint test check` + node units green; `govulncheck ./...` exit 0 (the persistent informational `x/crypto/openpgp` finding only); `make e2e` full parallel suite green (24 test cases across 13 spec files, incl. the new `entry-keyboard` spec).
- **p12.6 keyboard grid wiring** (follow-up to the FINDING above, user green-lit) — RESOLVED the "`nextCell` imported but never called" gap. **Pure layer (`txngrid.js`, node-tested):** `nextCell` gained a sixth param `isVisible(row, col) -> bool` (default `() => true`, so every prior test/behavior is byte-identical). `advance`/`retreat` became scanning loops that step forward/backward one cell at a time and **skip** cells where `isVisible` is false until they hit a visible cell or a grid boundary; each step strictly increases (forward) / decreases (backward) the position, so the scan provably terminates even on a fully-hidden row (guarded by a node test). Enter's add-row is now driven off advance's boundary result ("no visible cell forward on the last row") rather than a literal `col === cols-1`, so it stays correct even when trailing columns are hidden. New node tests cover: forward-skip over hidden program/class (fund→memo), backward-skip (memo→fund), Enter add-row at the last **visible** cell (incl. a trailing-hidden grid), Alt+Arrow unaffected by visibility, and the fully-hidden-row no-loop. **Column model:** the editable cells, in DOM/tab order, are mode-dependent — signed mode `account, amount, fund, program, class, memo` (6 cols); DR/CR mode `account, dr, cr, fund, program, class, memo` (7 cols, the amount split into two visible columns). The hidden `split_id` sink, the DR/CR-mode hidden signed `amount` sink, and the row-error `<td>` are **not** focus columns. `program` is visible iff the row's account type ∈ {revenue, expense}; `class` iff expense — the SAME `rowReveal` decision `gateRow` uses to toggle `visibility` (single source of truth, no duplication). **DOM glue (`txneditor.js`, e2e-covered):** a `keydown` handler is attached to `.txn-grid` (NOT the form) — structurally scoped, because the payee autocomplete and date inputs live in `.txn-header` **outside** the table, so the grid handler can never fight their existing keydown handlers. It parses the focused input's stable id (`txn-<field>-<i>`) → `{row, col}` via the column model (ids never move, so this is the focus↔cell bridge), builds `isVisible` from `rowReveal`, calls `nextCell` with `mods.ctrl = ctrlKey || metaKey` (Cmd≡Ctrl per the module header), and dispatches: `move`→focus the target input (but at a boundary where target==current it does **not** preventDefault, so native Tab carries focus out to the Save/Add-row controls — no focus trap); `add-row`→existing `addRow()` then focus the new row's first cell; `save`→`form.requestSubmit(submitBtn)` (fires htmx + native validation, unlike `form.submit()`); `cancel`→click the Cancel `<a>`; `move-row-up/down`→swap the two rows' field **values** (ids stay stable) then re-gate/recompute and follow focus. Enter is intercepted (preventDefault on move/add-row) so it never leaks a stray native form submit. **e2e (`entry-keyboard.spec.js`):** a second test drives the wired shortcuts with real `page.keyboard` — Enter advances account→amount→fund→**memo** on an asset row (asserting focus never lands on `#txn-program-0`/`#txn-class-0`), Shift+Tab/Tab prove the same skip backward/forward, Alt+ArrowDown reorders two rows (values swap, focus follows), and Ctrl+Enter from a mid-grid cell saves (HX-Redirect to the register). Stable across 20× parallel repeats of the keyboard spec (`--repeat-each=20 --workers=4`, 40 executions) + 20× full-suite parallel runs (`--workers=4`), zero flakes. Note (UX call for the orchestrator): Escape-as-cancel is wired per the task. Empirically probed (Chromium): Escape while a native `<select>` **popup is open** stays on the editor — the popup swallows the key so the grid handler never fires (no data loss on that path); Escape only cancels when a grid **text input** is focused (the intended Appendix C behavior). It still navigates away from a half-entered grid with no confirm — revisit if that feels too abrupt. DR/CR (7-col) column mapping is verified by inspection against the template cell order only — node tests use abstract grids and the e2e runs in the default signed mode — so DR/CR keyboard traversal is not test-covered.

- **p13.1** — **My settings** (`/settings`, AnyUser): the personal-preferences page consolidating language, date/number formats, display mode (signed vs DR/CR), negative style, theme, and default subsidiary into ONE versioned write. **No schema change** — the p06.1 migration already added every column (`locale`, `date_format`, `number_format`, `display_mode`, `neg_style`, `theme`, `default_subsidiary_id`) with a `users_versions` twin, and format.go already maps them per-user; p13.1 only PERSISTS them. **Versioned update:** new sqlc `UpdateUserSettings` is the LIVE update ONLY (positional `?`, ASCII); the store method reuses the existing snapshot-from-live `InsertUserVersion` inside one `write(ctx,"user.settings",...)` so the op=update snapshot captures all seven columns naming the actor (matches `SetUserTheme`). `cuento check` stays clean. **Validation set (the store is the guard — the migration puts a CHECK only on `txn_perm`, so these columns have NO DB backstop):** `date_format` ∈ ISO/US/EU; `number_format` ∈ US/EU/plain; `display_mode` ∈ signed/dr_cr; `neg_style` ∈ minus/parens; `theme` ∈ auto/light/dark (reused `ValidTheme`); `locale` via an injected `localeOK` closure (the web layer passes `known`/`i18n.Langs`, keeping the store i18n-free). Any unknown → `ErrInvalidSetting` BEFORE the funnel opens (so a reject writes no version row). **Default-subsidiary clearing:** nullable — an empty form value clears it to NULL; a non-empty id is existence-checked in-store (`GetSubsidiary`) so a bad id is a clean 422, not an FK-triggered 500. **Form convention:** a FULL-PAGE form (like `/admin/org`, not an inline htmx swap): success → 303 PRG to `/settings?saved=1` so the new locale/theme render on the fresh GET; invalid (only reachable by a crafted POST past the fixed `<select>`s) → whole page re-rendered at 422 with the p10.3 `formErrors`+autofocus adapted to a full page (no partial region to swap). **Theme cookie on save:** `resolveTheme` reads the `cuento_theme` cookie BEFORE the DB, so the POST also re-sets the cookie (exactly like `setTheme`) or the next render would show the stale cookie theme; locale needs no such step (`resolveLang` reads the DB for logged-in users). The header theme control (POST /theme) and login language switcher are UNCHANGED (shell.spec relies on them). **AUDIT (the "all render paths through p03 formatters + {{t}}" clause) — clean, nothing to fix:** grepped web handlers + templates for `time.Format`/`strconv`/`.Format`/raw amounts and hardcoded English. Every raw `.Format("2006-01-02")` produces the ISO-CANONICAL string the store consumes (as-of dates, cursors, query params) — NOT display; every user-facing date goes through `money.FormatDate(_, dateFormatFor(u))` (accounts `AsOf`, programs `From`/`To`, register/fund rows) and every amount through `money.Format` into pre-formatted `string` model fields; user-facing text is `{{t}}`. Only the `-dev` styleguide has literal color-swatch names (a component gallery, never production, not app content). **Tests:** `TestFormatsFollowUserSettings` posts one 1,234.50 debit dated 2025-03-15 and GETs the SAME register as two read users through the real router — user A (US/ISO/signed) body has `1,234.50`+`2025-03-15`+no DR/CR; user B (EU/EU/dr_cr) body has `1.234,50`+`15/03/2025`+a DR/CR marker and NOT `1,234.50`, plus `bodyA != bodyB`. `TestLocaleSwitchSwapsChrome` renders /settings for an es user and asserts es strings in BOTH the sliced `app-nav` (Ajustes/Cuentas) and `<main>` (Idioma/Formato de fecha/Tema), `lang="es"`, and that raw keys (`nav.settings`, `settings.language`, …) do NOT leak. Store tests assert the versioned round-trip (actor named, default-sub set then cleared) and per-field rejection (no version row on reject). e2e `settings.spec.js` switches locale→es + display→dr_cr, saves (full-page 303 to a DIFFERENT URL → `waitForURL(**/settings?**)`, never the no-op same-url pattern; no `waitForFunction` under the strict CSP), and asserts the es chrome on a subsequent /accounts page. Stable 20x under `--repeat-each=20 --workers=4`.
 **e2e worker-scoped isolation:** the `server` fixture is WORKER-scoped (one db shared across a worker's tests), so unlike the theme (a per-context cookie) a durable locale change to the SHARED admin poisons sibling tests in the same worker (txn-editor's English "New account" selector fails when the admin is left in es). settings.spec therefore RESTORES the admin to the seeded defaults (en/signed) before finishing; verified 5x full-suite green (26 specs) + 20x settings.spec parallel.
 The restore uses value-based/language-independent selectors (selectOption + `form.settings-form button[type="submit"]`, never a label regex) so a mid-test failure cannot cascade es-selector confusion; NO other spec file was modified (the txn-editor flake was purely this poisoning, not a latent race).

- **p13.2 (web: admin)** — The admin surface (`/admin/**`, all Admin perm) adds users management, per-user permissions, and currencies; the `/admin` stub becomes a real section index linking users/subsidiaries/currencies/org (org settings reuse p11.4 unchanged). Non-obvious calls:
  - **txn_perm enum = `none`/`read`/`write`** (the migration 00006 CHECK). The new versioned `SetUserTxnPerm` validates against this set (`store.ErrInvalidTxnPerm`) BEFORE the write — the column CHECK is only a backstop, mirroring the p13.1 settings validators. The web select offers exactly these three; the store is the guard.
  - **Last-admin guard keys on OTHER-enabled-admin count, not `CountHumanUsers`.** `CountHumanUsers` is `WHERE id<>1` (a non-system count) — using it would let "5 viewers + 1 admin" pass and lock out all admin access. `DisableUser` now loads the target and, if it is an admin, counts ENABLED admins OTHER than it (`CountOtherEnabledAdmins`); zero → `ErrLastAdmin`. The system user (id 1) can never be disabled (`ErrSystemUser`). The guard lives in the STORE so both the web handler AND the CLI `user disable` honor it (the CLI only ever disables non-admins in its tests, so unaffected). The web maps `ErrLastAdmin` to a page-level 422 (the guard is shown, nothing executed — like subsidiaries' `ErrHasActiveChildren`).
  - **System user (id 1) is unmanageable → detail routes REDIRECT to the list.** `TestRouteRegistryComplete` substitutes `{id}`→1 for parameterized routes; id 1 is the seeded system user, which the store's `AdminUserByID`/`SetUserTxnPerm`/grant writers refuse (`ErrSystemUser`). Returning 404 for id 1 would fail reachability ("not mounted"). So `GET /admin/users/{id}`, `POST .../txn-perm`, and `POST .../grants` map `ErrSystemUser` to a **303 → /admin/users** (it exists but is off-limits; a 303 is non-404 and non-login-302, so both the matrix and reachability tests pass), while a genuinely-unknown id stays a **404** (`ErrUserNotFound`; reachability never substitutes an unknown id). disable/reset also redirect the system user to the list, so all five treat it uniformly. This preserves rule 8 (routes auto-included from the registry) without special-casing the test.
  - **Currency management is NOT versioned** — `currencies` is static reference data (D1), absent from Appendix A's versions list and from the `cuento check` Z5 union (no `currencies_versions` twin). So `AddCurrency`/`SetCurrencyActive` are plain reference-data writes OUTSIDE the write funnel (no actor, no `changes` row), exactly like `SyncReportGroups`/`SetOrgSetting`. `AddCurrency` is an idempotent UPSERT (a re-add refreshes metadata + re-enables) so an admin re-adding an existing code — or a same-worker e2e retry — never PK-collides. Shape (3 uppercase letters, exponent 0..4, non-empty symbol/name) is validated in the store (`ErrInvalidCurrency`); the handler uppercases the code first.
  - **Grant model = composite-key versioned set, mirroring account_subsidiaries.** `user_report_grants_versions` keys `entity_id = user_id` with `group_name` as a snapshot column and part of the identity. `GrantReportGroup` guards with `HasReportGrant` (a re-grant is a no-op, no duplicate PK / no spurious version row), inserts live, then snapshots op='create'; `RevokeReportGroup` INVERTS the order — snapshot op='delete' BEFORE the live delete (the row must still exist to snapshot). The per-user page submits the FULL desired checkbox set; the handler diffs against current grants and issues one versioned grant per added group + one versioned revoke per removed group. Each perm/grant change is one `changes` row naming the acting admin (`store.Actor` from context) — precisely what `TestPermChangeVersioned` asserts: it uses `AssertVersioned`/`AssertVersionedGrant` for op + change existence, then `LatestVersionActor`/`LatestGrantActor` for the actor (the op helpers deliberately do not check `actor_id`), over a grant→revoke→re-grant round trip (create→delete→create).
  - **Password create/reset reuse `internal/auth.Hash` (argon2id, rule 13)** — the same credential path login uses; never hand-rolled. Users are never deleted (disable only). A duplicate username surfaces as `store.ErrUsernameTaken` (typed from the UNIQUE constraint) → a 422 username field error, not a 500.
  - **e2e isolation:** `admin.spec.js` logs in as the shared `e2eadmin` but creates and mutates its OWN uniquely-named user and adds a currency with a UNIQUE random 3-letter code, so nothing durable leaks into sibling specs on the worker-scoped shared db. Language-independent selectors (ids, form selectors, `data-username`/`data-code` row attributes); no `waitForFunction` (strict CSP). Full parallel suite verified 20× green, zero flakes.
- **p14.1 (db+store: rates)** — `exchange_rates(rate_date, base, quote, rate REAL, source, change_id, PK(rate_date, base, quote))` (migration `00013`), FKs base/quote→`currencies(code)` and change_id→`changes(id)`, plus index `(base, quote, rate_date)` for the on-or-before lookup. Non-obvious calls:
  - **`exchange_rates` is CHANGE-ID-ANCHORED, NOT versioned.** A rate is a fact about a day, not an editable entity with a reconstructable history, so there is no `exchange_rates_versions` twin, no trigger, no snapshot-from-live append — it is absent from Appendix A's versions list and from the `cuento check` Z5 union (like `currencies`/`report_groups`/`org_settings`). But UNLIKE those pure-reference tables, a rate load IS audited: `PutRates` runs through the store write funnel so a whole batch shares ONE `changes` row, recorded directly in `change_id` (who loaded these rates, when). **Z-invariant awareness: NONE needed** — this is the deliberate design decision the step flagged. Z5 checks `versions → changes` only; NO check asserts the reverse (`changes → some version`), because reference-data writes bypass the funnel entirely and even p06.1's system-user backfill only added a version row to keep Z5 clean, never a changes-coverage rule. So a rates-only `changes` row (referenced by no `*_versions` row) trips nothing. Proven empirically by `TestExtendRatesKeepsLedgerClean`: after `ExtendRates` loads 18 audited rate rows, `ledger.Check` still returns 0 Errors + exactly the fixture's 1 Z19 warning. `make check` (fresh migrated empty db, no rates) stays clean.
  - **`RateOn(base, quote, date) (store.RateResult, error)`** where `RateResult{Rate float64, RateDate string, Reciprocal bool}`. Resolution order: (1) `base == quote` → identity `1.0` with `RateDate = date`, **no row needed** (an identity rate is never stale, so the date is immaterial to footnotes); (2) a **DIRECT** `(base,quote)` row with the greatest `rate_date <= date` (`RateOnOrBefore` = `ORDER BY rate_date DESC LIMIT 1`) — direct **always wins**, even over a later reciprocal, because only a genuine `sql.ErrNoRows` falls through; (3) else a **RECIPROCAL** `(quote,base)` row on-or-before (same query, swapped args) → `1/rate` with `Reciprocal=true` and the inverse row's ACTUAL date; (4) else `ErrRateMissing`, wrapped with `%w` so p15 callers `errors.Is` it to footnote a conversion gap rather than fail. **Staleness:** `RateOn` returns the row's REAL `rate_date`, never echoes the query `date`, so a report says "as of <rate_date>" when the newest rate predates the query (`TestRateStaleness`).
  - **`PutRates(ctx, []store.Rate)` = "batch, one change".** Every row of a batch is inserted in a loop inside a SINGLE `s.write(ctx, "rates.put", …)` call, so all rows share the funnel's one `change_id` (`TestPutRatesOneChange` asserts `COUNT(DISTINCT change_id) == 1` across the batch and that a second batch is a distinct change). An empty batch is a no-op — no rate rows AND no `changes` row (nothing to audit; `TestPutRatesEmptyNoChange`). `InsertRate` is an idempotent UPSERT on the PK so a ratesync retry / manual correction over the same day+pair overwrites (refreshing rate/source and re-anchoring `change_id` to the loading batch) rather than PK-colliding. `rate` is a `REAL`/`float64` per the PLAN spec and D12 (the only place float64 is allowed, rule 3); conversion/rounding is p15's, not here — inserts are plain positional `?` (no `INSERT…SELECT`, so the p04.2 numbered-param quirk does not apply); `rates.sql` kept pure ASCII.
  - **Fixture seam `ExtendRates` (p14 seam filled).** Loads **18 monthly USD→MXN points** 2025-01-01 (17.00) → 2026-06-01 (18.10) via `PutRates` (one audited batch, source `"fixture"`), spanning the fixture's transaction range. The i-th rate is the EXACT linear interpolation `17.00 + i*(18.10-17.00)/17` — a fixed, re-computable formula (no clock/network). **OPT-IN:** `New` does NOT call `ExtendRates`, so the default fixture stays native-currency and every existing native-currency expectation is untouched; `f.Expected.Rates` is the zero value until a p15 test opts in. **Converted expectations are UNROUNDED floats** (`RatesExpected.ConvertedFundBalances`): p15 owns the rounding mode + rules (D12), so pinning rounded int64 here would pre-commit p15 to reproduce this step's arithmetic. The documented conversion RULE p15 must conform to: report base USD; an MXN balance converts at the **CLOSING** rate on-or-before AsOf (2026-06-30 → the 2026-06-01 point, 18.10) via the reciprocal `1/18.10` (`RateOn(MXN,USD,AsOf)`); USD funds pass through 1:1. `TestExtendRatesConvertedFundBalances` checks the derivation is internally consistent with the native `FundBalances`.
- **p14.2 (tools: ratesync + CSV import)** — The two rate-loading paths (auto-fetch + manual backfill) over p14.1's `PutRates`. Non-obvious calls:
  - **`RateSource` seam isolates the FRAGILE Yahoo dependency (new package `internal/rates`).** Yahoo Finance's chart endpoint is UNOFFICIAL and WILL break someday, so `cuento ratesync` depends only on `rates.RateSource` (`Fetch(ctx, []rates.Pair) ([]store.Rate, error)`); the Yahoo implementation lives behind it in `internal/rates/yahoo.go` (`Yahoo{BaseURL, Client}` + `NewYahoo()` with a 15s timeout). The blast radius of a Yahoo shape change is one file + one parser, never the command/store/tests.
  - **The PARSER is the tested unit; NO network in `go test` (AGENTS testing convention + DATA RULE 11).** `parseYahoo(pair, body) → store.Rate` is exercised entirely against SYNTHETIC, hand-authored bodies in `internal/rates/testdata/` (`yahoo_usdmxn.json`, `yahoo_empty.json`, `yahoo_error.json`) — NOT real captured payloads. `TestYahooParse` asserts the right (base, quote, value) AND that `rate_date` is DERIVED from the body's `regularMarketTime` (Unix seconds) rendered in **UTC** (`time.Unix(t,0).UTC().Format("2006-01-02")`), never the machine clock — the recorded `1704067200` → `2024-01-01`. Defensive because the endpoint is unofficial: empty result / Yahoo error channel / missing price → typed `ErrNoData` (not a panic, not a zero rate); malformed JSON → a wrapped decode error (`TestYahooParse{Empty,Error,Malformed,MissingPrice}`). Pointers on `regularMarketPrice`/`regularMarketTime` distinguish "field absent" from a real 0. The HTTP shim is thin (`BaseURL` settable so a test could point at a local server; a per-pair `ErrNoData` is SKIPPED so one delisted symbol never sinks the batch, but a transport/non-200 error aborts the whole fetch).
  - **CONFIGURED PAIRS = ACTIVE currencies × the ORG BASE, identity skipped (zero-config, derived — NOT an org-settings key).** `ratesyncPairs` takes the org base from the ROOT subsidiary's `base_currency` (`SubTree(ctx)[0]`, pre-order so the root is first; empty tree → a clear error, no index panic) — because report base currency FOLLOWS the scoped subsidiary (D18), it is INTENTIONALLY not an `org_settings` key (see `org_settings.go`). Pairs = `{(base, c) : c ∈ active currencies, c ≠ base}`, direction base→quote matching Yahoo's `USDMXN=X` symbol build and `exchange_rates`'s base→quote multiplier. Adding an active currency auto-extends coverage; nothing to maintain (`TestRatesyncPairsFromCurrencies`).
  - **`cuento ratesync` (`cmd/cuento/ratesync.go`, wired in `main.go` dispatch + usage).** Same open-the-db convention as the other subcommands (`openStore`, `-db` flag). `runRatesync(ctx, st, src)` derives the pairs, does ONE `Fetch`, and `PutRates` the whole batch as ONE change under the system actor (`store.WithActor(ctx, systemActor)` bound INSIDE the func, mirroring `user.go`, so a source error writes NOTHING — `TestRatesyncSourceErrorNoWrite` proves the no-partial-write; `TestRatesyncWritesFetched` proves the happy path via `RateOn`). No network in any test (a `fakeSource` injects canned results). **The systemd timer that runs this unattended is documented in phase 18 (p18), NOT built here** — only referenced.
  - **Admin CSV upload (`/admin/rates`, Perm Admin) — the FIRST multipart upload in the app.** `ratesImport` uses stdlib `r.ParseMultipartForm`/`r.FormFile` (1 MiB cap) + `encoding/csv`. **All-or-nothing:** `parseRatesCSV` validates the exact header `rate_date,base,quote,rate,source` and EVERY row (`FieldsPerRecord`=5; `rate_date` strict `YYYY-MM-DD`; base/quote uppercased + known-currency check; `rate` a positive float; non-empty `source`) and builds the FULL `[]store.Rate` batch BEFORE any write — the first bad thing returns a `csvError{key, arg}` (i18n key + offending line number) and `PutRates` is never called, so a partial import can't leave the books half-loaded (`TestRatesCSVImport`: a valid file imports N rows verified via `RateOn`; a bad header AND a bad data row after a valid one each 422 with nothing written). `PutRates` runs under `s.actorCtx(r.Context())` (the funnel needs the acting admin; the batch is one change). Reuses the form-error convention as a **page-level 422** (a file input can't be echoed back on re-render, so no inline partial swap — same shape as the currencies add-error). New i18n keys `admin.rates.*` in BOTH catalogs (`imported` is a `tn` plural; `admin.rates`/`admin.rates.error` are prefixes only, no leaf/prefix collision). The matrix test picked up the two new `/admin/rates` routes automatically (rule 8).
  - **No new dependencies** (stdlib `net/http`/`encoding/csv`/`encoding/json`/`mime/multipart` only); `go.mod`/`go.sum` unchanged. **e2e** `e2e/tests/rates-import.spec.js`: an admin adds two OWN uniquely-coded currencies then uploads a rates CSV between them on a random far-future date (worker-scoped-db isolation — nothing shared is touched) via `setInputFiles` with an in-memory Buffer, and sees the imported-count confirmation; a bad-header upload shows the error, not a crash. `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); the full parallel e2e suite (28 specs) green 20×.
- **p15.1 (reports: framework)** — The report FRAMEWORK every Phase-15 report (p15.3–p15.11) plugs into: a report SHAPE + the machine renderer in a new `internal/reports` package (no HTTP, no html/template — so it never imports `web`, avoiding a `reports→web→reports` cycle), and the generic HTTP/HTML glue in `web`. Non-obvious calls:
  - **Report SHAPE = pure data + a Run func:** `reports.Report{ID, TitleKey, Group, ParamsSpec, Run(ctx, *Toolkit, Params) (Table, error)}`. `ID` is the URL slug + registry key (lowercase ascii/digits/-/_, unique); `TitleKey` an i18n message id (rule 9 — the package renders NO text); `Group` a permission bucket; `ParamsSpec` declares which shared params the report consumes; `Run` is a pure read (rule 2, no funnel). The `Registry` (`reports.NewRegistry`/`Default`) is iterable and PANICS on a malformed registration (empty/bad id, dup id, empty title, undeclared group, nil Run) — a build-time defect surfaced at startup like `mustParseTemplates`, never at request time. `reports.Default()` assembles the shipped set (today: the smoke report); p15.3+ each add one `reg.Register(...)` line and nothing else changes.
  - **The report-GROUP set (replaces the p06.3 `_placeholder`):** `reports.Groups() = {financial, funds, programs, tax}` — small, aligned to the Phase-15 categories by AUDIENCE/permission need (financial statements p15.3–6+9; funds p15.8; programs p15.10; IRS-990/tax p15.7+11), NOT by data source, so an org can grant a bookkeeper the statements without the 990 package. `web.codeReportGroups()` now returns `reports.Groups()` (the web layer still owns the sync because route Perms reference the names); `SyncReportGroups` upserts ALL declared names, so a group can exist before any report references it. p13.2's admin grant UI now shows the real four (raw names, no i18n — the grant checkbox renders `.Name` verbatim, confirmed). `routes_test.go`'s `placeholderReportGroup` constant → `grantedReportGroup = "financial"` (the smoke report's group) and the ReportsOnly persona is granted it — so the existing `TestPermissionMatrix` proves granted→200/ungranted→403 on the real report route with ZERO new matrix code.
  - **Routes auto-mount CONCRETE per report, not a `/reports/{id}` wildcard (the load-bearing routing choice):** `routes()` iterates `s.reports.All()` and appends a literal `Route{GET, "/reports/"+id, ReportGroup(group), reportPage}` AND `"/reports/"+id+".csv"` per report. Concrete (not wildcard) because (a) each report needs its OWN group Perm, which one wildcard route can't carry; (b) `routes_test.go`'s `concreteURL` substitutes `{seg}→"1"`, so a wildcard `/reports/{id}` would resolve to bogus id `1` and 404 the completeness check; (c) an unknown `/reports/{id}` then simply never matches a route and the mux 404s on its own (rule 8: the whole surface is these declared routes). Because the routes are appended to the SAME registry the matrix iterates, every report route is permission-tested automatically (the "appears in the matrix" requirement). The handler derives WHICH report from the request path (last segment minus `.csv`), since concrete routes carry no `{id}` PathValue.
  - **Table = typed cells, rendered by BOTH a generic HTML template and the CSV writer:** `reports.Table{Columns[], Rows[]}`; a `Cell` is `CellText` (a stored proper noun rendered VERBATIM) / `CellLabel` (an i18n message KEY the renderers localize) / `CellMoney` (int64 minor + ISO currency, rule 3 — EXACT, never pre-formatted) / `CellDate` (ISO string); a `Row` carries `Indent` (tree depth), a `RowKind` (`RowData/RowSubtotal/RowTotal/RowWarning`), where `RowWarning` is the D19 intercompany net-nonzero row (rendered `role="alert"`, never dropped). **The label/proper-noun distinction is EXPLICIT at the cell kind (`TextCell` vs `LabelCell`), NOT guessed from the string** — a deliberate foundation choice (advisor-flagged): nine reports (p15.3–11) inherit it, and a 990-preparer report must never render a raw i18n key nor wrongly translate an account name. `renderCell` (HTML) localizes `CellLabel` via `i18n.T`, renders `CellText` verbatim, and formats money/date per the user's settings (rule 10); the CSV path resolves `CellLabel→CellText` once (`localizeLabelCells`) before the i18n-free `WriteCSV`. ONE generic `report.tmpl` renders any Table, so p15.3+ never touch a template or the handler.
  - **CSV = stdlib `encoding/csv`, machine-plain + correctly escaped:** `reports.WriteCSV(w, table, localize, exps)` uses `csv.Writer` (RFC 4180 quoting), money in `NumberPlain` (no grouping to fight the delimiter), dates as ISO, headers localized via the caller's `{{t}}`. `TestCSVEscapingRoundTrips` builds a cell holding `a,b"c\nd` (comma + quote + newline), writes, re-parses with `csv.Reader`, and asserts byte-identity — plus `-50.00` (signed) and a Blank money cell → empty string (distinct from a formatted zero). `reportCSV` streams it as `text/csv` with a `_smoke.csv` attachment filename.
  - **Shared params form — SCOPE always, the rest gated by ParamsSpec:** rendered on EVERY report (`report.tmpl`) as a plain `<form method="get">` (no JS, strict CSP — rule 12) submitting query params back to the same report. The **subsidiary SCOPE selector is UNCONDITIONAL** (`select[name=scope]`, D18 — every report is scoped) and is what `TestScopeSelectorOnEveryReport` asserts across `reports.Default().All()` (so a p15.3+ report is covered automatically). Defaults (`resolveParams`): scope = the user's `DefaultSubsidiaryID` else the root (`SubTree[0]`, pre-order); as-of = today; period = year-to-date; target currency = the scope subsidiary's `base_currency` (D18). Date inputs are plain text (never `input[type=date]` — rule 10/12), formatted per the user's setting. `ParamsSpec.{AsOf,Period,Granularity,Currency}` toggle the remaining controls.
  - **Toolkit = store + resolved params; p15.2 fills the methods.** `reports.NewToolkit(st, params)` holds the read-only store and the run's `Params`. p15.1 exposes `Store()` so the smoke report reads a REAL balance (`SubtreeBalancesAsOf` on the resolved scope) end-to-end; p15.2 GROWS the Toolkit with the Appendix-E methods (`BalancesAsOf/Activity/Rollup/NetIncome/FundBalances/FunctionalMatrix/ProgramActivity/Group990/IntercompanyNet`) over the p08.4 queries with D12 conversion + descendant-closure consolidation (D18) + D19 collapse, and real reports (p15.3+) call THOSE instead of `Store()` (kept a narrow accessor so the migration is a compile-time nudge).
  - **The ONE example report is a THROWAWAY placeholder (`_smoke`), NOT the trial balance.** `reports.SmokeReportID = "_smoke"` (leading underscore marks it internal/placeholder; title key explicitly labelled "placeholder") exercises typed cells (text + money + indent + subtotal), both renderers, the route, and the matrix — proving the framework end to end without pre-empting p15.3 (which OWNS the `trial-balance` id and shape). A later step deletes `smoke.go` + its `Default()` registration and the framework carries on unchanged.
  - **"Registry sync creates groups" is asserted at the DB layer** (`TestReportGroupsSynced`): after the startup `SyncReportGroups`, `store.ReportGroupNames` returns EXACTLY the four `reports.Groups()` in declared order — not just the one group the smoke report references — so `funds`/`programs`/`tax` land in `report_groups` before any report uses them (a grant FK is valid the moment p15.3+ or an admin references them).
  - **TDD deviation recorded (AGENTS DoD):** the `internal/reports` package tests were written alongside implementation and the first run was GREEN (the core types are pure data + small functions with no prior symbol to fail against), rather than observed-failing-first. The web-layer tests (`reports_test.go`) and the required listed tests (unknown-id-404, scope-selector-on-every-report, CSV escaping, group sync) DID drive behavior and are the real coverage; the deviation is the package-internal unit tests only.
  - **No new dependencies** (stdlib `encoding/csv` only); `go.mod`/`go.sum` unchanged; no sqlc/query changes. New i18n keys `reports.*` in BOTH catalogs (`reports.params.*`, `reports.gran.*`, `reports.export.csv`, `reports.empty`, `reports.smoke.*` — group NAMES are raw reference values, not catalog entries; no dotted-key/leaf collision). `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); the full parallel e2e suite (29 specs incl. the new `reports` spec — opens the smoke report, asserts the params form + scope selector + table, downloads the CSV) green 20×.

- **p15.2 (reports: toolkit)** — The Appendix-E computation core (`internal/reports/compute.go`) that all nine Phase-15 reports (p15.3–p15.11) call: consolidation (D18), conversion (D12), intercompany collapse (D19), effective-990 rollup (D25), placeholder tree rollups — in ONE place, so a report never re-derives them. Non-obvious calls:
  - **Method signatures (grown on `*Toolkit`, all pure reads, rule 2), dates as STRINGS:** `BalancesAsOf(ctx, Scope, asof, ConvertOpts) (map[AccountID][]CurAmt, error)`; `Activity(ctx, Scope, from, to, ConvertOpts) (map[AccountID][]CurAmt, error)`; `FundBalancesAsOf(ctx, Scope, asof, ConvertOpts) (map[FundID][]CurAmt, error)` (fund id 0 = unrestricted, D20); `FunctionalMatrix(ctx, Scope, from, to, ConvertOpts) (map[AccountID]map[Class][]CurAmt, error)`; `ProgramActivity(ctx, Scope, from, to, ConvertOpts) (map[ProgramID]map[AccountID][]CurAmt, error)`; `Group990(ctx, part, currency, map[AccountID]int64) ([]LineRow, error)`; `IntercompanyNet(ctx, Scope, asof) ([]CurAmt, error)`; `NetIncome(ctx, Scope, from, to, ConvertOpts) (CurAmt, error)`; `Rollup(ctx, currency, map[AccountID]int64) ([]TreeRow, error)`; `ByPeriod(from, to, Granularity, func(pFrom, pTo string) error) error`; `RateOn(ctx, base, quote, d) (Rate, error)`; plus `ConvertMinorAt(ctx, minor, from, to, d) (int64, error)` and exported `RoundHalfEven(float64) int64`. Types: `Scope{Sub SubsidiaryID}`, `ConvertOpts{To string; Mode RateMode(None|Closing|TxnDate)}`, `Class string`, `CurAmt{Currency string; Minor int64}`, `LineRow{Code; Amount CurAmt; Unmapped bool}`, `TreeRow{AccountID; Name; Amount CurAmt; Indent; Subtotal bool}`, `Rate{Rate; RateDate; Reciprocal}`.
  - **Appendix-E DEVIATION (recorded):** the sketch names `ledger.Date` and sketch return types (a bare `CurAmt`/`Rate`/`LineRow`); there is NO `ledger.Date` type — the p08.4 balance queries and `RateOn` already take **YYYY-MM-DD strings**, so the toolkit does too (matching the actual store API is the right call; Appendix E is explicitly a sketch). The value types (`CurAmt`, `Rate`, `LineRow`, `TreeRow`) are defined in the reports package. Balance/activity methods return a `[]CurAmt` per key (one entry per native currency; a converting mode collapses to a single target-currency entry). `Group990` takes an already-converted leaf map (single currency) so it does no conversion. Ids are plain `int64` aliases (`AccountID`/`FundID`/`ProgramID`/`SubsidiaryID`).
  - **Consolidation needs NO scope expansion here (D18):** the p08.4 queries ALREADY close the subsidiary descendant set with a recursive CTE (p08.4 note), so every method forwards `Scope.Sub` straight to the store. Root scope = full org; a leaf = just that sub. Proven by `TestBalancesAsOfRootVsLeafScope`: at ROOT every `Expected.AccountBalances` reproduces exactly; at the MX-leaf scope the MX-only accounts keep the SAME native balance (single-sub accounts are scope-invariant) while US-only accounts (CheckingUS/Building/DueFromMX/CreditCard) are ABSENT — the discriminating descendant-closure check.
  - **Conversion (D12) rounds HALF-EVEN at the FINAL AGGREGATE, via `money.ConvertMinor`** (the existing exponent-aware `math.RoundToEven` primitive — the ONLY float touch, rule 3). Grain: `Mode:Closing` converts EACH output cell (per fund/account per currency) once at the as-of on-or-before rate (`RateOn`), then sums the int64 results — the same grain the p14.1 seam blesses (`ConvertedFundBalances` are per-fund unrounded floats "p15 rounds"). `Mode:TxnDate` decomposes the period into calendar MONTHS via `ByPeriod(GranMonth)`, accumulates the UNROUNDED `Σ(monthNative·monthRate·10^(expTo−expFrom))` per output cell, and `RoundToEven`s ONCE — never per-month int64 rounding (that would round intermediately, violating "at final aggregates"). Under on-or-before lookup with the fixture's monthly rate schedule, month-granularity IS exactly per-transaction-date (every txn in month M resolves to the M-01 point). A missing rate surfaces `store.ErrRateMissing` (wrapped in-chain) so a report footnotes the gap, not a silent zero.
  - **Conversion oracles (hand-computed, fixture-derived):** `TestFundBalancesClosingConversion` — BecaAgua MXN 9,700,000 ÷ 18.10 = 535,911.60.. → **535,912**; unrestricted MXN 30,940,000 ÷ 18.10 = 1,709,392.26.. → **1,709,392**; USD legs pass through (asserted against `Expected.Rates.ConvertedFundBalances`). `TestActivityTxnDate` — Food Purchases MXN activity 120,000@2025-03 + 90,000@2025-04 + 150,000@2026-03 at rates 17.1294.../17.1941.../17.9059.. → unrounded 20,616.978.. → **20,617 USD**; PLUS a **grain-lock** on Cash MXN (whose monthly activity makes the two grains DIVERGE: per-month-rounded = 38,971, accumulate-then-round-ONCE = 38,970 — asserts **38,970**, discriminating the correct final-aggregate grain from per-month rounding; verified failing under a per-month-rounding mutation). `TestNetIncomeClosing` — native R/E subtotals USD −3,567,500 + MXN −9,140,000; MXN→USD ÷18.10 = −504,972.37.. → −504,972; total **−4,072,472 USD**. `TestRoundHalfEven` pins the tie-to-even + sign symmetry.
  - **Effective 990 code (D25):** `Group990` reuses the store's `Effective990Codes` walk (own code, else nearest ancestor's), rolls the leaf map by effective code (absent → `""` = the explicit **Unmapped** bucket), orders rows by `form990_lines(sort,code)` (via `codeOrder`, unioning the account-type-filtered line reads) with Unmapped ALWAYS last. `TestGroup990Effective` (Part IX): Salaries→IX.7, Occupancy→IX.16, **Bank Fees→IX.11g (LEAF OVERRIDE lands on its own line, NOT folded into the Expenses parent's IX.24e)**, Program Supplies+Insurance+Event Costs inherit IX.24e = 370,000. `TestGroup990Unmapped` (Part VIII): Event Income (the fixture's deliberately-unmapped R/E leaf, Z19) is the LAST row, code `""`, `Unmapped=true`, −300,000 — never dropped.
  - **Intercompany (D19):** `IntercompanyNet` reads the flagged account ids (new `IntercompanyAccountIDs` store method + ASCII positional query) and sums THEIR as-of balances per currency across the consolidated scope. `TestIntercompanyNetBalanced` → 0 USD on the clean fixture (Due-from +1,000,000 nets Due-to −1,000,000, `Expected.IntercompanyNetCurrencies`). `TestIntercompanyNetCorrupted` builds the nonzero case CORRECTLY: you can't post an unbalanced txn (zero-sum enforced) and net-to-zero is a CROSS-transaction property, so it posts ONE extra **balanced** txn (DR Due-from +250,000 / CR Checking US −250,000) with no mirroring Due-to credit — each txn stays zero-sum, but the consolidated intercompany net becomes +250,000 USD → the caller renders a `RowWarning`.
  - **Program rollup (D24):** `ProgramActivity` folds each program's raw cells UP the program tree (a parent includes descendants), so General (the seeded root) carries the whole org's program activity while leaf programs (Educación/Food Pantry) equal the raw `Expected.Program` oracle. `TestProgramActivity` asserts leaves against the oracle and General's hand-computed folded totals (e.g. Program Supplies USD 60,000 General-raw + 150,000 Educación = 210,000; Food Purchases MXN 210,000 + 150,000 = 360,000). `Rollup` walks the ACCOUNT tree pre-order emitting a `Subtotal` row per placeholder (subtree leaf sum) in tree order + leaf data rows; `TestRollupTreeOrder`: the Expenses placeholder subtotal = 2,327,500 (all expense leaves) and precedes its children.
  - **TDD (observed):** the tests carry hand-computed fixture-derived numbers; breaking the rounding primitive (`RoundToEven`→`Floor−1`) fails `TestRoundHalfEven`/`TestNetIncomeClosing`/`TestFundBalancesClosingConversion`/`TestActivityTxnDate`, confirming they are load-bearing (not tautological). No report pages/templates/goldens/e2e (those are p15.3+). Currency exponents cached per run (`Toolkit.expCache`). ISO date arithmetic (`ByPeriod` month decomposition) uses `time.Parse("2006-01-02")` — internal compute, not a user-render path (rule 10 governs display), the same pattern `store.transactions` validates dates with.
  - **One sqlc query added** (`IntercompanyAccountIDs`, ASCII, positional, `make gen` reproducible); no other query changes; no new dependencies; `go.mod`/`go.sum` unchanged. `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists).

- **p15.3 (report: trial balance) + the Phase-15 GOLDEN-FILE harness** — The FIRST real financial report (`internal/reports/trial_balance.go`), replacing the p15.1 `_smoke` placeholder, plus the golden convention p15.4–p15.11 copy. Non-obvious calls:
  - **`_smoke` is FULLY removed.** `internal/reports/smoke.go` deleted; `reports.Default()` now registers `registerTrialBalance` (id `reports.TrialBalanceReportID = "trial_balance"`, group `financial`, `ParamsSpec{AsOf, Currency}`) instead of the smoke report; the `reports.smoke.*` i18n keys are gone from BOTH catalogs (replaced by `reports.trial_balance.*`); `web/reports_test.go`'s three smoke tests are repointed to trial balance (a real balancing table over the seeded 2-account posting) with the `trial_balance.csv` download filename; `e2e/tests/reports.spec.js` targets `/reports/trial_balance`. `routes_test.go`'s remaining `_smoke` mentions were COMMENTS ONLY (the matrix iterates real routes and the ReportsOnly persona's `financial` grant covers trial balance automatically — zero logic change); the `grp="placeholder"` in the decide-logic unit table is unrelated and untouched.
  - **Row model = one row per (account, currency); columns Account | Currency | Native | Converted.** A multi-currency account (FX Clearing holds USD 9,740 AND MXN 5,000) can't collapse into one money cell, so each (account, currency) is its own row. An EXPLICIT Currency text column is mandatory because the CSV writer emits money as a bare number with NO currency code (native "5000.00" vs a USD converted "276.24" would otherwise be ambiguous in the golden). Balances are **net-debit SIGNED** int64 minor units (D2: assets/expenses +, revenue/liability/equity −) in a single column — the task's "or signed per the net-debit convention"; no debit/credit split columns. Rows are emitted in ACCOUNT-TREE PRE-ORDER (walking `store.Tree(ctx, lang, nil)` and looking up each account's balances) so the output is DETERMINISTIC — `BalancesAsOf` returns a map, and Go map iteration is randomized, so relying on its order would flake the golden; currencies within an account are sorted.
  - **Converted column = per-cell `ConvertMinorAt`, a documented DEVIATION from the task's "use `BalancesAsOf(RateClosing)`".** `BalancesAsOf(RateClosing)` COLLAPSES a multi-currency account to ONE summed target-currency cell, which destroys the per-currency row alignment this report needs, so the report calls `tk.ConvertMinorAt(ctx, amt.Minor, amt.Currency, target, asof)` per (account, currency) cell instead. The arithmetic is IDENTICAL — `convertClosing` (what RateClosing uses) IS `ConvertMinorAt`-per-currency-then-sum — so this is a presentation choice, not a numeric one (D12 half-even at the final cell is preserved). Same toolkit, same rule.
  - **Lang plumbing: a new `Params.Lang` field (the pattern p15.4+ inherit).** This is the first report to render STORED per-language account names (`account_names`, resolved via `store.Tree(ctx, lang, …)`). `Run(ctx, tk, p)` has no `lang`, the lang ctx key is web-package-private, and AGENTS forbids smuggling anything but the actor through ctx — so `Params` grew a `Lang` field the web layer sets from `langOf(ctx)` in `resolveParams`, read via `p.LangOr()` (defaults "en" so a hand-built zero Params still resolves names). Every p15.4+ report that names accounts uses this.
  - **The trial balance BALANCES (asserted, the whole point):** the native per-currency SIGNED sum is exactly 0 (integer double-entry, D2). `TestTrialBalanceGolden` re-sums the report's OWN emitted native cells (`reports.SumMoneyColumn` over DATA rows — so a dropped/duplicated row is caught, not just the report's own total rows) and asserts **USD sum = 0 AND MXN sum = 0**, and that exactly those two currencies appear. The report emits a `RowSubtotal` "Total (native)" row per currency (each 0) plus a `RowTotal` "Total (converted)" grand total.
  - **HAND-VERIFIED converted figures (derived from the fixture natives + the 2026-06-30 closing USD→MXN rate 18.10, MXN→USD = 1/18.10, half-even at the cell — NOT read from code):** Checking MX 39,500,000 MXN → **2,182,320 USD**; Government Grants −10,000,000 MXN (its MXN leg) → **−552,486 USD**; Food Purchases 360,000 MXN → **19,890 USD**. The CONVERTED grand total is **0** on this fixture — the per-account MXN rounding residuals CANCEL — but that is a data property (asserted only after an in-test re-sum confirms it), NOT a guarantee: a future report whose residuals don't cancel would need a translation-adjustment line. A report is a trial balance because its NATIVE per-currency sums are zero; the converted total is presented so a reviewer sees the statement reconciled in one currency.
  - **GOLDEN HARNESS (the reusable Phase-15 deliverable, `internal/reports/golden.go` + `internal/reports/testdata/`):** two committed, human-REVIEWABLE artifacts per report — `<id>.txt` (an aligned fixed-width column dump via `reports.DumpTable`: the readable native+converted side-by-side view a reviewer scans, with `>`/`=`/`!` row-kind markers) and `<id>.csv` (the machine export via `reports.WriteCSV`). Both are DETERMINISTIC: the golden test PINS lang=en, as-of 2026-06-30, root scope, USD target, and formats money with a FIXED `reports.GoldenMoneyOpts` (NumberUS/Minus/Signed — NOT per-user settings, so no locale drift). Regeneration: a `-update` test flag rewrites the files; `make golden` runs `go test ./internal/reports/ -run Golden -update` (the Makefile stub is now implemented) — verified idempotent (a second `make golden` produces NO diff). The golden ENCODES CORRECTNESS: the key totals (native 0 per currency, converted 0, the three converted cells) are hand-derived from the fixture and asserted BEFORE the file comparison, so the golden isn't just a snapshot of whatever the code emits. DATA RULE 11: every value derives from the synthetic Appendix-D fixture (via `f.ExtendRates` for the rate column), so the goldens are safe to commit. p15.4–p15.11 copy this pattern (their own `testdata/<id>.{txt,csv}`, the same `checkGolden`/`DumpTable`/`-update`/`make golden` mechanism).
  - **Scope test reuses the toolkit consolidation:** `TestTrialBalanceScope` runs the report at ROOT vs the RV Mexico LEAF (native mode, rate-independent) and asserts the account SETS differ — MX-only accounts (Checking MX, Cash MXN, Due to RV Internacional) appear at both, US-only accounts (Checking US, Building, Due from RV Mexico, Credit Card) appear at root but are ABSENT from the MX leaf (descendant closure of MX excludes US, D18), and the leaf set is a strict subset. `TestTrialBalanceCSVParses` re-reads the report's CSV with `csv.Reader` and re-sums the native column to zero per currency — the balancing property survives the export.
  - **No new dependencies** (stdlib `encoding/csv`, `flag`, `os`, `path/filepath` only); `go.mod`/`go.sum` unchanged; no sqlc/query changes. New i18n keys `reports.trial_balance.*` in BOTH catalogs (title/col.account/col.currency/col.native/col.converted/total_native/total_converted — `reports.trial_balance.title` is a leaf, never a bare `reports.trial_balance` prefix-and-leaf collision). `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists). **e2e** `e2e/tests/reports.spec.js` (repointed): an admin opens the trial balance, sets as-of/scope params, sees the params form + scope selector + table + a balancing total row, and the `.csv` endpoint returns `text/csv` — STRUCTURAL assertions (the fresh worker db has no seeded ledger, so specific numbers would be brittle), READ-ONLY (no worker-db leak). Full parallel suite green 15×.

- **p15.3d (reports: drill-down — framework capability)** — Every report balance/activity figure "clicks through" to the transactions that produce it: a filtered split list whose signed NATIVE sum EQUALS the figure. Built framework-FIRST so p15.4–p15.11 and the Phase-19 budget reports inherit it; only the trial balance is retrofitted here. Non-obvious calls:
  - **The `Drill` shape (`internal/reports/drill.go`) is pure, data-only, query-string-serializable.** `Drill{Scope int64, AccountIDs []int64, Currency string, FundID/ProgramID/Class *…, Mode DrillMode (DrillAsOf|DrillPeriod), AsOf/From/To string}`. It reconstructs ONE cell's figure: the scope (descendant closure D18), the account SET the cell sums (one id for a leaf cell; a subtree for a future rollup cell), the cell's NATIVE currency (REQUIRED for a money cell), and optional fund/program/class narrowing. `Mode` picks the date treatment (as-of cumulative vs period activity) so the drilled split set matches the toolkit method that produced the cell. `Encode()` → a stable-key query string; `DecodeDrill(url.Values)` is its FORGIVING inverse (a malformed id/date is dropped, so a tampered URL degrades to an empty list — a 200, not a 500 — and the permission matrix's BARE `/reports/{id}/drill` hit decodes to a zero drill = empty result). It carries NO store/HTTP, so the reconciliation invariant is unit-testable and the CSV/text renderers ignore it entirely (**golden UNCHANGED** — confirmed by `make golden` no-diff; drill links live only in the HTML `Cell.Drill` path).
  - **A `Cell` gains an optional `*Drill` + a `WithDrill(d)` builder.** `MoneyCell(m, ccy).WithDrill(&Drill{…})` opts a cell in; a nil `Drill` = not drillable (labels, totals). Keeping it a method (not a `MoneyCell` param) means the many non-drillable call sites are untouched and only drillable cells opt in — the p15.4+ attach pattern. The HTML renderer (`web/renderCell`, now threaded `reportID`) emits `renderedCell.Href = "/reports/"+id+"/drill?"+Drill.Encode()`; `report.tmpl` renders a drillable cell as a plain `<a class="report-drill-link" href=…>` (strict CSP — NO inline handler). CSV/`DumpTable` read only `Text`/`Minor` and never see `Drill`.
  - **PER-REPORT drill route + STATIC perm (not a dynamic-perm endpoint) — the rationale.** `routes.go` auto-mounts `GET /reports/{id}/drill` alongside the existing page + `.csv`, gated by the SAME `ReportGroup(report.Group)` as the report. So drill visibility EQUALS report visibility ("a viewer who can see the number can drill it"), and — being a CONCRETE registry route — it is permission-matrix-checked with ZERO test edits: `TestPermissionMatrix` hits `/reports/trial_balance/drill` bare (decodes to an empty drill → 200 empty list for an authorized persona, 403 for an ungranted one). A single dynamic `/reports/drill?…` endpoint was REJECTED — its Perm couldn't be static per report-group, so the matrix couldn't cover it. The drill handler needs its OWN path parse (`reportDrillFromPath`: strip `/reports/` prefix + `/drill` suffix); the existing `reportFromPath` strips `.csv` and would mis-parse `{id}/drill`.
  - **The store query (`DrillSplits`, `internal/db/queries/balances.sql` + `store/balances.go`) MIRRORS `SubtreeBalancesAsOf`/`PeriodActivity`, NOT `RegisterPage`.** The critical correctness point: the trial balance at ROOT scope sums an account over the subsidiary DESCENDANT CLOSURE (the recursive `scope` CTE), and one account can carry splits across several subs in that closure. `RegisterPage` filters ONE subsidiary by equality — reusing it would UNDER-count at root scope and break reconciliation. So `DrillSplits` uses the SAME recursive `scope(id)` CTE + `t.subsidiary_id IN (SELECT id FROM scope)`, `t.deleted = 0`, filtered to `sp.account_id = ?` AND **`t.currency = ?`** (each toolkit cell is per-currency — FX Clearing holds USD AND MXN, so only the currency-filtered sum equals a single cell), an as-of OR period date bound, and optional fund/program/class — returning the raw UNGROUPED splits ordered `(date, split_id)` (the store sums them). Optional filters use the paired-`?` active-flag trick (`(? = 0 OR col = ?)`) because sqlc's sqlite parser mangles a reused `?` (p04.2). A zero `AccountID` short-circuits to no rows (the empty/matrix hit). `balances.sql` stays pure ASCII (p04.2); `make gen` reproducible.
  - **Handler + render reuse the register row shape.** `web/reports_drill.go`: decode → `store.DrillFilter` → `DrillSplits` per account (bounded: one today) → `renderDrillRows` (mirrors `registerRows`' name-map + counter-account resolution: date/sub-badge/payee/memo/counter-or-"Split"/fund-chip/amount) → `report-drill.tmpl`, each row linking to the txn editor + history (p12.4). The header echoes the RECONCILED figure — the signed sum of the SAME rows shown — so header and list are provably consistent.
  - **RECONCILIATION invariant (the key test, `web/reports_drill_test.go` `TestDrillReconcilesToToolkit`):** for selected trial-balance cells over the fixture at ROOT scope, `Σ DrillSplits.Amount == tk.BalancesAsOf(RateNone)` native figure for that `(account, currency)` — the toolkit is the oracle, not the drill's own output. Covers a single-currency account (Checking MX 39,500,000 MXN), a MULTI-currency account (FX Clearing MXN 500,000 — reconciles ONLY because currency is filtered; an explicit check proves its MXN ≠ USD 974,000 drill), and a fund/program/class-narrowed PERIOD drill (proving the optional filters + the From/To date mode don't silently drop rows). HTTP tests assert the route lists rows with editor/history links, echoes the figure, and 200s on the bare hit.
  - **CONVERTED/CONSOLIDATED cells drill to NATIVE splits (framework rule; retrofit drills the native cell only).** The reconciliation invariant is against the PRE-conversion native figure — a cell the report showed converted (D12) still lists the native underlying transactions; the drill header carries a `Converted` annotation (i18n `reports.drill.native_note`) for a later report that drills a converted column. The trial-balance retrofit attaches `Drill` to the NATIVE money cell only (the converted column is left non-drillable — the native drill already covers the transactions); `nativeDrill = &Drill{Scope: p.Scope, AccountIDs: []int64{node.ID}, Currency: a.Currency, Mode: DrillAsOf, AsOf: p.AsOf}`. **p15.4–p15.11 attach `Drill` the same way** — a `MoneyCell(…).WithDrill(&Drill{…})` on their balance/activity cells, `DrillPeriod` (From/To) for activity reports, `FundID`/`ProgramID`/`Class` set for narrowed cells.
  - **No new dependencies** (stdlib `net/url`, `sort`, `strconv`, `strings`); `go.mod`/`go.sum` unchanged. New i18n keys `reports.drill.{title,figure,native_note,empty}` in BOTH catalogs (all leaves under `reports.drill.*`, no dotted-key/leaf collision). `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `make golden` no-diff. **e2e** `e2e/tests/reports.spec.js` (+1 test): seeds a balanced transfer (two unique-named accounts + one txn), opens the trial balance at the default as-of, clicks a `.report-drill-link`, lands on the drill list, and sees rows each linking to the editor/history — STRICT CSP (no `page.waitForFunction`), mutating but only ADDING uniquely-named rows (inert to sibling reads). Full parallel suite green **15×** (450/450).

- **p15.4 (report: balance sheet)** — The statement of financial position (`internal/reports/balance_sheet.go`), the second real financial statement, copying the p15.3 golden + p15.3d drill patterns. Non-obvious calls:
  - **Net-asset classification is BY RESTRICTION, and the identity is STRUCTURAL, not lucky (the accounting trap).** Sections are ASSETS, LIABILITIES, NET ASSETS. Assets/Liabilities list their account rows (net-debit signs, D2: assets stored positive; a liability's stored credit is NEGATED to read as a positive balance). NET ASSETS is the nonprofit split (Q3, D20): **Net assets with donor restrictions** = Σ the RESTRICTED funds' asset-side (unexpended) `FundBalancesAsOf` balances (a fund is restricted when its `Restriction` field is non-empty — purpose/time/perpetual; fund id 0 excluded), via a new `Toolkit.restrictedNetAssets`. **Total net assets** = the PLUG `(Assets − Liabilities)` taken from the already-collapsed section totals — NOT a fund-balance sum. **Net assets without donor restrictions** = total − with. This is deliberate: `FundBalancesAsOf` is ASSET-side only (a whole-fund sum is zero by conservation), so it sums to total ASSETS, not net assets — a naive "without = fund 0" is off by exactly the liabilities. **The discriminator (asserted in `TestBalanceSheetNativeSplit`):** on the fixture USD, without = 17,217,500 while fund-0 = 18,517,500 (differ by the 1,300,000 of USD liabilities); on MXN they accidentally coincide (30,940,000) because MXN liabilities are zero — so a "without = fund 0" bug passes MXN and fails USD, and the test asserts USD without ≠ 18,517,500 explicitly. Because NA is the plug of the collapsed sections, the identity `Assets = Liabilities + (without + with)` holds by construction and stays consistent through intercompany collapse and FX conversion.
  - **Equity-source accounts are NOT emitted as rows.** Only `asset`/`liability`-type accounts get rows; `equity`/`revenue`/`expense` accounts (Opening Balances, the R/E leaves) are ABSORBED into the net-asset plug. Emitting Opening Balances (−18,700,000 USD equity) alongside the synthetic by-restriction lines would double-count and break the identity (net assets decompose two ways — by restriction {without+with} and by source {opening+surplus} — both summing to 22,267,500; the E section shows the BY-RESTRICTION split only). A test asserts no `Opening Balances` row appears.
  - **"Net surplus to date" is a DISCLOSURE, not a peer of the totals.** It is `NetIncome`-from-inception (cumulative R/E activity to the as-of date, presented POSITIVE since a surplus is a net credit), rendered as an indented "of which" memo under without-restriction and NOT summed into any total (a new `Toolkit.netSurplusByCurrency`; `inceptionDate = "1900-01-01"` as the lower bound — on-or-before every possible txn, so it is truly cumulative-from-inception without per-org config). Hand-verified on the fixture: 3,567,500 USD / 9,140,000 MXN.
  - **Intercompany collapse + warning (D19), gated on CONSOLIDATED (multi-sub) scope.** D19 says "collapse flagged accounts", so at a consolidated scope the intercompany-flagged due-to/due-from accounts are ELIMINATED — dropped from the Assets/Liabilities LISTINGS and their section totals (not shown as rows), via `store.IntercompanyAccountIDs` skipped in the classification walk. On the balanced fixture they net to zero, so total net assets is unchanged and only the Assets/Liabilities totals shrink by the eliminated 1,000,000 USD each (root golden: Total assets 248,128.04, Total liabilities 3,000.00, NA 245,128.04 — identity holds). The residual (`Toolkit.IntercompanyNet`, computed only at a consolidated scope) is surfaced as a `RowWarning` (`role="alert"`, never hidden) when NONZERO. **Scope gate (a bug fixed on advisor review):** "consolidated" = the scope's descendant closure has >1 subsidiary (`Toolkit.isConsolidated` via `store.Descendants`). At a LEAF/single-sub scope the intercompany accounts are that sub's GENUINE due-to-parent / due-from-child balances — SHOWN as ordinary rows, NOT collapsed, and NEVER warned (an earlier version warned at leaf scope because `IntercompanyNet(leaf)` sees only one side, −1,000,000 ≠ 0 — a false positive; `TestBalanceSheetScope` now pins `warningRow(leafTable) == nil` AND that Due-to is shown at the leaf but collapsed at root). Warning tested clean-vs-corrupted (`TestBalanceSheetIntercompanyWarning`) via p15.2's corrupted-copy technique: post ONE extra BALANCED txn (DR Due-from +250,000 / CR Checking US −250,000) with no mirroring Due-to credit — each txn stays zero-sum but the consolidated intercompany net becomes +250,000 USD → the warning row appears carrying the 250,000 residual. Absent on the clean fixture.
  - **Per-currency DETAIL toggle = a new framework param.** `Run` only sees `Params`, so the toggle is a new `Params.Detail` string (`""` = converted-only, `"currency"` = per-currency) gated by a new `ParamsSpec.Detail`, parsed in `web/resolveParams`, surfaced as a `<select name="detail" class="report-detail-select">` in `report.tmpl` (mirroring the Currency/Granularity wiring). Converted-only mode = 2 columns `[Line, Amount(converted at AsOf closing rate, D12)]`; detail mode = 4 columns `[Line, Currency, Native, Converted]` expanding each line per native currency (the first currency row carries the label, continuations blank-named). The **net-asset total is the plug of the NATIVE sections, then converted per-currency once at the closing rate** (each currency converted once, then summed) — NOT per-line residual cancellation. The converted identity `Assets = Liabilities + NetAssets` holds on this fixture (asserted by the hardcoded converted `wantConv`), which is why the golden pins the converted totals directly rather than relying on `round(A−L) == round(A)−round(L)` in general. Recorded as a framework touch (a fourth optional shared param alongside AsOf/Period/Granularity/Currency).
  - **Drill (p15.3d) on asset/liability ACCOUNT cells only.** Each account cell carries a `Drill{Scope, AccountIDs:[id], Currency, Mode:DrillAsOf, AsOf}` (the trial-balance retrofit shape) reconciling to the account's native figure; in converted-only mode a single-currency account drills its one currency, a multi-currency account (FX Clearing) is left non-drillable there (one link can't reconcile a summed-across-currencies converted figure — the detail view is its drill path). The synthetic net-asset lines are NOT drillable (they span many funds/accounts, no single `DrillFilter`). Reconciliation proven in `web/reports_drill_test.go` `TestBalanceSheetDrillReconciles`: it runs the REAL report, pulls every drillable cell's `Drill`, and asserts `Σ store.DrillSplits == tk.BalancesAsOf` native for each (account, currency) — the toolkit is the oracle, not the report's output.
  - **Golden = converted-totals mode at root scope (the multicurrency, multi-sub case).** `internal/reports/testdata/balance_sheet.{txt,csv}` at lang=en, as-of 2026-06-30, root (US+MX / USD+MXN consolidation), USD target. Key totals hand-derived from the fixture natives + the 18.10 closing rate BEFORE the file comparison, POST intercompany elimination (converted USD minor: Assets 24,812,804; Liabilities 300,000; NA 24,512,804; without 18,926,892; with 5,585,912; surplus 4,072,472; L+NA = Assets). The detail mode is covered by `TestBalanceSheetDetailToggle` + `TestBalanceSheetNativeSplit` (native per-currency figures asserted directly against the oracle) rather than a second golden. A leaf-sub (RV Mexico) vs root scope difference is `TestBalanceSheetScope`.
  - **No new dependencies** (stdlib `context`, `sort`); `go.mod`/`go.sum` unchanged. New i18n keys `reports.balance_sheet.*` (title, `col.{line,amount,currency,native,converted}`, `section.{assets,liabilities,net_assets}`, `total.{assets,liabilities,net_assets,liabilities_net_assets}`, `na.{without,with,surplus_of_which}`, `intercompany_warning`) + `reports.params.detail` + `reports.detail.{converted,currency}` in BOTH catalogs (all leaves, no dotted-key/leaf collision). `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `make golden` no-diff. **e2e** `e2e/tests/reports.spec.js` (+1 test): opens the balance sheet, asserts the three section labels + the with/without net-asset split labels + a balancing grand-total row, toggles `detail=currency` (asserts ≥4 columns + the select value), and fetches the CSV — STRICT CSP (no `page.waitForFunction`), READ-ONLY (mutates nothing durable). Full parallel suite green **15×** (6 full-suite runs of 31 tests + 9 targeted `reports` runs of 3 tests, all passed, no flakes).

- **p15.5 (report: income statement / statement of activities)** — The period P&L (`internal/reports/income_statement.go`), the third real financial statement, copying the p15.3 golden + p15.3d drill patterns. Non-obvious calls:
  - **TXN-DATE conversion for a FLOW (D12), NOT closing.** The balance sheet (a position) converts at the AsOf CLOSING rate; the income statement is a FLOW — each revenue/expense figure is converted at the rate on each transaction's DATE (`Toolkit.Activity(scope, from, to, ConvertOpts{To, Mode: RateTxnDate})`, the p15.2 month-decomposition), because income earned across a period is measured at the rate in force when it occurred. The hand-verified discriminator (`TestIncomeStatementGolden`): Food Purchases (MXN) whole-range activity converts to **20,616 USD** at txn-date but **19,890 USD** at closing — the test asserts the total column reproduces 20,616 and is NOT 19,890, proving the mode.
  - **COMPARATIVE COLUMNS via `ByPeriod`; the TOTAL column is Σ(period columns), NOT a fresh whole-range Activity call (the FOOTING rule — advisor-flagged).** With month/quarter granularity the period splits into calendar sub-periods (`Toolkit.ByPeriod`, the deterministic bucketing), one COLUMN each, each column = that sub-period's `Activity`. Under `RateTxnDate` the per-account converted figure is rounded half-even ONCE per `Activity` call (`compute.go`), so `round(Σ months)` ≠ `Σ round(month)`: building the Total column by ADDING the per-period converted cells (int64, no rounding) makes "**Σ period columns == total column, per row**" hold EXACTLY (`TestIncomeStatementComparativeColumns`). A statement that does not foot is wrong, so footing WINS over bit-equality with a whole-range `NetIncome`. Concretely the report NET (sum-of-quarters) = **4,099,507** differs by ONE minor unit from whole-range `NetIncome`-txn-date (4,099,506); the report's figure is authoritative. The EXACT, rate-independent cross-check is the NATIVE R/E net: USD 3,567,500 / MXN 9,140,000 (net-debit, presented positive), asserted directly against the fixture oracle (`TestIncomeStatementNativeNet`).
  - **MIXED R/E TREE PRESERVED (not flattened).** `runIncomeStatement` walks the account tree per section (`section(tree, "revenue"|"expense", …)`): the Revenue and Expenses PLACEHOLDER parents are `RowSubtotal` rows carrying their subtree's per-column sums, their children are indented `RowData` rows. A leaf is in-section only when it is of the section type AND carries activity in the scope (present in the converted-total map) — so a global chart-of-accounts leaf with no in-scope activity drops out (drives `TestIncomeStatementScope`: US-posted Contributions appears at root, absent at the RV-Mexico leaf). **Sign per section:** revenue activity is net-debit NEGATIVE (a credit) → display sign −1 (positive inflow); expense is net-debit POSITIVE (a debit) → sign +1; net surplus = −(revRaw + expRaw) = Revenue − Expense, positive = surplus, per column (`RowTotal`).
  - **Period COLUMN HEADERS are stable period IDENTIFIERS, not formatted dates.** A comparative column's header is `YYYY-Qn` (quarter) / `YYYY-MM` (month) — a period NAME (a coarse column marker), deterministic across locales, so it sidesteps rule 10 (which governs date/number RENDERING, not period identifiers) and needs no framework change to the header path (`Column.HeaderKey` surfaces an unknown key verbatim via `i18n.T`'s fallback). GranNone → a single "Period" column. The Total/section/net labels are real i18n keys.
  - **Drill (p15.3d) on single-native-currency R/E leaf cells, `DrillPeriod`.** Each drillable leaf cell carries `Drill{Scope, AccountIDs:[id], Currency, Mode:DrillPeriod, From, To}` — From/To = that column's sub-period range (or the whole [From,To] for the Total cell). Multi-currency leaves and all subtotal/net rows are non-drillable (one link can't reconcile a summed-across-currencies or rolled-up figure — the balance-sheet rule). Reconciliation proven in `web/reports_drill_test.go` `TestIncomeStatementDrillReconciles`: it runs the REAL report, pulls every drillable cell's `Drill`, and asserts `Σ store.DrillSplits == tk.Activity` NATIVE for each (account, currency) over that sub-period range (the report figure was CONVERTED but the drill lists NATIVE splits; the invariant holds against the pre-conversion native figure). Anchor: Food Purchases MXN whole-range drill = 360,000.
  - **Golden = quarterly comparative columns at root scope (`internal/reports/testdata/income_statement.{txt,csv}`)** at lang=en, period 2025-01-01..2026-06-30, USD target, txn-date. Six quarterly columns + Line + Total = 8 columns; key totals hand-derived from the fixture natives + the p14.1 monthly rate schedule BEFORE the file comparison (converted USD minor: Total revenue 6,476,594; Total expenses 2,377,087; Net 4,099,507; and the per-leaf totals). A leaf-sub vs root difference is `TestIncomeStatementScope`.
  - **No new dependencies** (stdlib `context`, `fmt`; `cuento/internal/store` for the tree-row shape — the same import `compute.go` already uses); `go.mod`/`go.sum` unchanged. The web layer needed ZERO changes — the shared params form already renders Granularity/Currency/Period generically from `ParamsSpec` (p15.1). New i18n keys `reports.income_statement.*` (title, `col.{line,period,total}`, `section.{revenue,expenses}`, `total.{revenue,expenses}`, `net`) in BOTH catalogs (all leaves, no dotted-key/leaf collision). `make lint test check` + `govulncheck ./...` exit 0 (the p06.1 informational `x/crypto/openpgp` finding persists); `make golden` no-diff. **e2e** `e2e/tests/reports.spec.js` (+1 test): opens the income statement at a fixed period + `granularity=quarter`, asserts the Revenue/Expenses/Net labels (the R/E tree), the comparative-column header count, a net (report-total) row, the granularity/from/to controls, and fetches the CSV — STRICT CSP (no `page.waitForFunction`), READ-ONLY. Full parallel suite green **15×** (32 tests/run, all passed, no flakes).
